# 研究结论索引

这份文档是**实验记录**，不是教程。它回答三个问题：测过什么、结论是什么、
哪些结论站得住。详细推导在 `README.md` 对应章节。

最后更新：2026-09-19。测试基线 561 个。

---

## 一、总览

策略在**可测的每一个维度**上都被随机方向或买入持有支配，除了最后一项。

| # | 方向 | 结论 | 状态 |
|---|---|---|---|
| 1 | 鲸鱼因子（40% 权重） | 效果在两段之间翻转符号（PF 2.00 → 0.83）；2026-09-23 复核：**四个配置与「权重归零」全部不可区分**（配对差异区间跨 0） | **无据** |
| 2 | 对齐门禁 | 只影响 2 笔交易，不可测 | **不可测** |
| 3 | 入场周期（1h → 15m） | 无改善，且不产生更多样本 | **无据** |
| 4 | 离场参数（5 种配置） | 随机基准全部跑赢策略 | **无据** |
| 5 | 入场信号（时序 IC） | 相关系数符号翻转，均值约 0 | **无信息** |
| 6 | 账户持仓多空比 | 有效独立样本仅 15–24，不可测 | **不可测** |
| 7 | **横截面反转（非流动标的）** | **t = −4.4 ~ −6.1，通过成本/退市/随机三重检验** | **有平均效应，但组合结果不稳定** |
| 8 | 分散票池（31 标的，ρ≥0.7） | 有效观测 64 → 177，但 PF 1.22 → 1.10；11/31 超 p75（期望 7.75，p=0.13） | **无据** |
| 9 | **四项纯趋势硬门槛**（24h 涨幅 / 量能 / SMA20 / EMA 金叉，0–4 票） | 9 格中 7 格亏钱，**0 格跑赢买入持有**，1 格超 p75（期望 2.25） | **无据** |
| 10 | **手续费与换手率**（跨全部策略的量） | 主策略毛 PF 1.29 → 净 1.22（**−12.5pp**）；趋势门槛 −14~16pp。降换手后净值由负转正，但对照显示是「更像长期持有」而非更好的择时 | **成本是主导项，此前从未单独报出** |
| 11 | **回调入场**（24h≤3% + EMA 上穿中线 + 放量） | ETH +29.56%（敞口 19.8%）超买入持有，p100；分段 7/18 超 p75（期望 4.5，p≈0.09）。**但仍不显著**，且 BTC/SOL 的结果全部来自单笔最佳交易 | **目前唯一值得继续的规则** |
| 12 | 换成 EMA9/26 金叉 + 0~3% | 头部 ETH +35.90% 更好看，但**分段掉到 4/17、4/18（等于随机）**；收益全部来自单笔，ETH 两变体的最好一笔是同一笔 +26.17% | **比 §二.11 更差**：它把规则从反转侧挪回延续侧 |
| 13 | 从图反推的「脉冲+回踩+金叉」 | 规格与图不符（图上 EMA9 低于 EMA26，金叉未发生）；加上下文后**成交只剩 2–12 笔**，结果比裸金叉更差 | **在这份数据上无法评估**：事件本身只有 5–20 次/170 天 |
| 14 | AAVE 15m「收复中轨但未金叉」 | 上涨窗口中 **0/6 超 p75，六个全负，五个里四个毛收益已为负**（−1.8% ~ −17.0%，敞口仅 7%） | **无据，且不是成本问题**：信号本身没有毛优势 |
| **15** | **时序均值回归（RSI / Keltner）** | 45 标的：毛 **+446%** / 净 **+338%**，26/45 盈利；分段 **112/216 超 p75**（修正 P=0.009）；ADX<30 中位 +4.4%→**+9.1%** | **本仓库第一个毛收益为正、分段显著的规则**，但绝对收益低于买入持有 |
| 16 | 给 §15 加止损 | 左尾确实截断（均亏损 −5.1%→−2.5%），但**各段最优止损为 2%\|2%\|2%\|无\|无\|12%**，8% 从未胜出 | **测不出效应，不采纳**（上涨窗口代价明确） |
| **17** | **横截面版 RSI / stretch** | 非流动一侧：基线 `past_return` **+43.0%** > RSI **+27.5%** > stretch **+22.6%**（同向递减）；流动一侧全部无效 | **在既有效应范围内，弱于基线，不替代它** |
| **17b** | **时序版 vs 横截面版（2×2）** | 时序版**两半都有效**（非流动 48/86、流动 35/86），横截面版**只在非流动有效**（流动 −9.8%） | **时序版是独立效应，不是横截面版的劣化近似** |
| **18** | **非流动一侧是否微观结构假象** | hold=1 效应为**零**（−0.014%, t=−0.90）且随持有期增长；lag-1 只占负相关 **28–31%**；效应与价差不相关 | **不是假象**（P1/P2 被否定）。~~成本敏感~~ **见 19** |
| **19** | **真实成本口径（实盘 L2 盘口）** | 票池真实顶档价差 **3.04bp**（Roll 高估 10 倍）；$1k 规模 **7.2bp/边**、$10k **14.9bp/边**；50bp 内深度仅 **$18k** | **修正 18**：$1k 下效应存活（t=2.1–2.6），$10k 勉强，**容量是硬约束** |
| **20** | **时序择时 + 横截面选股叠加** | ρ = **−0.016**（五段一致，与独立不可区分）；等资本组合 **跑输**较优单独（1.21 vs 1.28），**风险配权**才提升（1.28→**1.56**） | **独立成立，但组合优势不成立**：全样本 1.56 是前半 0.06 与后半 2.81 的平均 |
| **21** | **跨窗口重复（6 个不重叠 69 天窗）** | **TS 超p75 在 6/6 窗口高于 25%**（合计 242/529=46%，符号检验 **P=0.031**）；XS t>2 仅 **1/6**；组合 Sharpe>1 仅 3/6 | **TS 择时优势复现（本仓库第一个）**；**XS 与组合不成立**。§二.20 用原始收益否定 TS 是换了尺子 |
| **22** | **出场扫描（成本实测后重做）** | 单窗口 exit=35 **77/86 (90%)** 远胜默认 50 的 56%（毛收益同样如此，对照 20/100/200 次都不变）；**跨窗口 exit=35 是 4/6、P=0.69，是最差的**；50/60/70 都是 **6/6、P=0.031** | **不采纳改动，保留 exit=50**。同一失败模式第三次出现 |
| **23** | **流动性五档 × 跨窗口** | 效应在 **Q1–Q4 全部 6/6（P=0.031）**，**Q5 不存在（4/6, P=0.69）**；而 Q1–Q4 的 50bp 深度只有 **$9k–27k**，Q5 有 **$80k**、价差 **1.08bp** 却无效应 | **效应的存在性与可收割性反向**。「扩容量」方向被否掉 |
| **24** | **成本敏感性（闭合测量）** | 「超 p75」在 0–50bp 上是**常数 43.6%**（构造上成本盲）；净收益中位在 **2.5bp/边过零**；**实测成本 7.2bp/边** | **择时技巧真实但值 2.5–4bp，成本 7.2bp —— 真实成本下不盈利。** 这是本方向的终点 |
| **25** | **降低执行成本这条路** | 七个出场阈值的跨窗口平衡费率为 **−119.5 ~ 5.53 bp**（最高 exit=55 的 5.53bp），**无一超过实测 7.2bp**；exit>60 后毛收益本身为负 | **缺口 1.3 倍**，但量级与测量误差同阶；**能动的是费率不是换手**（更正 §24 的错误估算） |

**只有第 7 项有可测的平均效应 —— 但分段之后，它的强度并不稳定**（见 §2.6）。

第 8 项是把「样本量不足」这个借口拿掉之后的检验：换一个真正分散的票池
（31 标的，池内 ρ̄ 从 0.859 降到 0.365），有效观测提升 2.8 倍，**效应仍然没有出现**。
所以第 1–6 项的「无据」不是样本不够造成的 —— 第六节那条「先提升统计功效」的
建议，现在已经被执行并且给出答案了。

区别很重要：**平均 IC 显著**（t = −4.4 ~ −6.1，有效样本 250–500）是可靠的；
**组合起来的收益**从 −0.05% 到 +191% 都在，6 段里只有 3 段超过随机第 75 百分位。
前者是「每期有一点点优势」，后者是「这点优势在复利下被方差主导」。

---

## 二、逐项记录

### 1. 鲸鱼因子（40% 权重）

| 组 | 权重 | 门禁 | 交易 | PF | 总盈亏 |
|---|---|---|---|---|---|
| A | 禁用 | 关 | 119 | 1.21 | +$48.72 |
| B | 0.40 | 关 | 69 | 1.43 | +$58.58 |
| C | 0.40 | 开 | 67 | 1.36 | +$48.33 |

钉住窗口后权重「看起来」帮忙，但**拆两段不重叠窗口后翻转**：

| 段 | 无鲸鱼 | 有鲸鱼 |
|---|---|---|
| H1 (3/29–6/15) | PF 1.43 | **PF 2.00** |
| H2 (6/16–9/15) | PF 1.09 | **PF 0.83** |

与上一轮 walk-forward 一致（样本外 0.79 vs 不用它 1.02）。

**门禁那一组不可测**：B 与 C 的 ETH/SOL 逐字相同，门禁只影响 BTC 的 2 笔。

#### 复核（2026-09-23）：把「权重归零」当成一次正式测量

上表的数字**复现不了**，而原因不是窗口差一天 —— 是配置变了。上表测于 2026-09-19
（`a36cdbf`），用的是 **conservative** 预设；`50d7fcb`（9-22）把配置切成 **balanced**：
入场阈值 60/40 → 55/45、`max_signals_per_day` 3 → 5、`cooldown_minutes` 240 → 120。
阈值变了，入场就变了，所以这不是「重跑」，是**在另一个策略上做的新测量**。

（顺带核实一条容易记混的：`risk_preset` **不含** `max_open_positions`。该字段从初始提交
`982d60a` 起就是 1，此后未被改动，也不在任何一个预设里 —— 预设放宽的是
`max_signals_per_day` 与 `cooldown_minutes`。它也不是「装不下多空篮子」的原因，见 §2.6。）

窗口仍是 `--as-of 2026-09-15` 的 170 天切成两半（79 + 91 = 170），3 币种池化，3.5bp/边：

| 窗口 | 权重归零 | ceiling=50（生产） | ceiling=10 |
|---|---|---|---|
| H1 (3/29–6/15) | **1.35**（88 笔） | 1.10（77 笔） | **1.52**（74 笔） |
| H2 (6/16–9/15) | 1.18（88 笔） | **1.46**（85 笔） | 1.16（85 笔） |
| 全窗口（170 天） | 1.22（175 笔） | **1.28**（160 笔） | — |

**符号仍然翻转，而且两段正好互换了**：

| | 9/19（conservative） | 9/23（balanced） |
|---|---|---|
| H1 | 开 2.00 > 关 1.43 | 开 1.10 < 关 **1.35** |
| H2 | 开 0.83 < 关 **1.09** | 开 **1.46** > 关 1.18 |

全窗口又给出「开略好」（1.28 vs 1.22）—— 与上表同一个陷阱，**全窗口是两个反向半段的平均**
（§2.20 是同一模式：1.56 = 0.06 与 2.81 的平均）。

##### 误差棒：本次测量的主要结论是「测不出」

PF 是点估计。新增 `--dump-trades` 导出逐笔 `net_pnl`，对每臂做 20,000 次引导法重采样：

| 臂 | 笔数 | PF | 95% 区间 | P(PF<1) |
|---|---|---|---|---|
| H1 归零 | 88 | 1.35 | [0.73, 2.21] | 15.5% |
| H1 ceiling=50 | 77 | 1.10 | [0.53, 1.89] | 39.8% |
| H1 ceiling=10 | 74 | 1.52 | [0.81, 2.57] | 9.2% |
| H2 归零 | 88 | 1.18 | [0.61, 1.96] | 29.9% |
| H2 ceiling=50 | 85 | 1.46 | [0.79, 2.41] | 10.1% |
| H2 ceiling=10 | 85 | 1.16 | [0.57, 1.95] | 33.0% |

**每一臂的区间都跨过 1.0。** 再做配对差异（同窗口两臂独立重采样）：

| 对比 | 中位差 | 95% 区间 | P(差>0) |
|---|---|---|---|
| H1 ceiling=50 vs 归零 | −0.25 | [−1.29, +0.75] | 30.9% |
| H1 ceiling=10 vs 归零 | +0.18 | [−0.96, +1.39] | 62.4% |
| H2 ceiling=50 vs 归零 | +0.27 | [−0.75, +1.36] | 70.7% |
| H2 ceiling=10 vs 归零 | −0.02 | [−1.00, +0.96] | 48.1% |

**四个差异的区间全部跨过 0**，P(差>0) 落在 30.9%–70.7%，≈ 抛硬币。

##### 结论

1. **40% 权重买不到任何可测的东西** —— 没有任何鲸鱼配置能与「权重归零」区分开。
2. **但「有害」同样不成立。** 上一轮读出的 H2 PF 0.83 在这批样本上不可分辨，
   「无据」比「有害」准确。
3. **基础策略本身也没被确立**：所有臂的 P(PF<1) 在 9.2%–39.8%，**包括权重归零那臂**。
   所以这不是「鲸鱼拖累了一个好策略」。
4. **旧的正向证据是配置特定的**：预设从 conservative 切到 balanced 之后不再成立。
   阈值动 5 个点就翻转，说明它读的不是稳定效应。

可复现命令：

```bash
python run_backtest.py --symbols BTC,ETH,SOL --days 79 --as-of 2026-06-15 \
    --candles-archive --fee-bps 3.5 --random-runs 20 --dump-trades /tmp/H1_off.json
# 带鲸鱼：再加 --whale-snapshots --whale-leaderboard --whale-max-fills-per-day {10,50}
```

**未做的一处（结论里最大的空白）**：§2.1 原先那个「随机顾问」对照**在 CLI 里没有对应
开关**，所以本次无法复现它 —— 也因此无法排除「鲸鱼只是减少了交易数」这个解释。
`--dump-trades` 正是为此加的：没有逐笔明细就做不了误差棒，而这个仓库已经记过一次
「PF 1.52 与 PF 1.03 落在同一个噪声带」。

### 2. 入场周期 15m

bar 数 4 倍（4,801 → 19,201），交易数 119 → 123。引擎计数器给出原因：
`signals_seen` 严格 4 倍（每 bar 触发率恒为 0.85），但 `threshold` 拦截率
74.4% → 88.5%，两者抵消。**所以 15m 不解决样本量问题。**

（最初以为门禁上限挡住了频率 —— 错。拦截明细里根本没有 `daily cap`。）

### 3. 离场参数

| 配置 | 交易 | PF | 策略 | 随机中位 |
|---|---|---|---|---|
| baseline 3/9 | 119 | 1.21 | +4.87% | +13.40% |
| tight 1.5/4.5 | 229 | 1.30 | +7.55% | +8.00% |
| wide 8/9 | 97 | 1.29 | +6.74% | +16.63% |
| no TP | 80 | 0.98 | −0.37% | +13.27% |

**五种配置里随机全都跑赢**，而随机用的是同一套离场参数。

### 4. 入场信号的时序 IC

| 窗口 | 样本 | corr(score, 未来24h) |
|---|---|---|
| W1 | 7128 | +0.0192 |
| W2 | 7128 | −0.0214 |
| W3 | 7128 | +0.0023 |

组件分解：技术因子、Supertrend 1h/4h、EMA 价差、RSI、动量**全部翻转符号**。
唯一稳定的是 ADX（+0.094 / +0.141），但它是**强度变量不含方向**，且规则检验失败
（样本内 +4.88% → 样本外 −7.62%）。

### 5. 账户持仓多空比

`positions` 的**名义**多空比恒等于 50.0%（永续合约多空名义必然相等）—— 零信息量。

按**账户数**算的在 47.4% ~ 70.3% 间变化，IC −0.038 ~ −0.169（9/9 全负）。但：

- 去趋势（滚动 z）后 IC 塌到 **−0.0180**
- lag-1 自相关 **0.88 ~ 0.92**，有效独立样本仅 **15 ~ 24**

**「9/9 全负」是同一段行情被数了九遍**，不到一个标准误。

### 6. 横截面反转 ← **唯一成立的一项**

161 个标的覆盖 350+ 天。横截面 IC 把有效样本从 15–24 提到 250–500。

| 信号 | 有效 N | IC | t |
|---|---|---|---|
| mom_24h | 464 | −0.0410 | **−4.07** |
| vol_72h | 284 | −0.0456 | **−3.33** |

**效应完全在非流动的一半：**

| 信号 | 子集 | t |
|---|---|---|
| mom_24h | 流动（81） | −1.07 |
| mom_24h | 非流动（80） | **−5.86** |
| vol_72h | 流动（81） | −1.03 |
| vol_72h | 非流动（80） | **−6.02** |

**五项挑战全部通过：**

| 检验 | 结果 |
|---|---|
| 成本（含 50bp 滑点） | 通过（50bp 仍 +20.85%） |
| 流动子集对照 | 干净（同规则 −55.88%） |
| 极端期依赖 | 通过（均值 ≈ 中位数） |
| 退市最坏情况（按归零） | 通过（+87.20%） |
| 随机多空对照（30 次） | 通过（100 百分位） |

**四项保留：** 只有一个 414 天样本；需要多空一篮子的组合架构；
IC 绝对值不大（收益来自横截面离散度 p10 −89% / max +783%）；
归档自身的完整性未验证。

#### 2.6 分段稳定性：平均效应成立，组合收益不成立

全样本那个 +87.20%（第 97 百分位）是**一个观测**。把同一票池定义
（第 `a` 行时存在的标的）应用到**不重叠的时间段**，每段独立做流动切分、
独立跟随机对照比较：

| 段 | 期间 | 标的 | 期数 | 净收益 | **中位/期** | 随机中位 | **百分位** |
|---|---|---|---|---|---|---|---|
| 半1 | 25-07-31~26-02-22 | 87 | 28 | +6.41% | +0.560% | −5.75% | **77** |
| 半2 | 26-02-23~26-09-17 | 96 | 29 | **+61.05%** | +2.313% | −8.66% | **100** |
| 段1 | 25-07-31~25-11-11 | 87 | 13 | **−0.05%** | +0.424% | −9.85% | **63** |
| 段2 | 25-11-11~26-02-22 | 92 | 14 | +48.01% | +1.726% | +2.65% | 100 |
| 段3 | 26-02-23~26-06-06 | 96 | 14 | +22.03% | +2.153% | −3.77% | 80 |
| 段4 | 26-06-06~26-09-17 | 150 | 14 | **+190.96%** | +2.770% | **+12.68%** | **57** |

**两个方向一致的观察：**

- **中位/期在 6 段里全部为正**（+0.424% ~ +2.770%），净收益 5/6 为正
  —— 符号是稳的，和「每期有一点点优势」一致。
- **但百分位不稳定**：77 / 100 / 63 / 100 / 80 / **57**。
  **段4 净收益 +190.96%，却只有第 57 百分位** —— 因为同期随机中位是 +12.68%，
  那 191% 是市场给的，不是信号给的。

**6 段里只有 3 段超过随机第 75 百分位。** 原假设下 `X ~ Bin(6, 0.25)`，
`P(X ≥ 3) ≈ 17%` —— **不显著**。

**所以正确的结论是这样拆开的：**

| 主张 | 证据 | 状态 |
|---|---|---|
| 横截面反转有**平均**预测力 | IC t = −4.4 ~ −6.1，n_eff 250–500 | **成立** |
| 按此构建的组合**能赚钱** | 6 段：−0.05% ~ +191%，3/6 过 75 百分位 | **不成立** |

**原因不是效应不存在，而是每期那点优势（中位约 +2%）在复利下方差太大。**
把「IC 显著」直接读成「策略可行」是这一步最容易被跳过的错误。

---

### 7. 标的间相关性：三个标的是 1.1 个赌注，不是 3 个

复盘时提出的问题：回测只用了 BTC/ETH/SOL，三者高度相关，池化它们的交易数
**等于把同一个赌注数了三遍**。实测确认，而且幅度比预期大。

**测得的相关系数**（1h 收益，170 天窗口）：

| | 值 |
|---|---|
| BTC-ETH | **+0.888** |
| BTC-SOL | **+0.838** |
| ETH-SOL | **+0.850** |
| 52 个流动永续平均两两相关 | +0.438 |

**池化样本量的真实规模**：

```
rho_bar = 0.859  ->  膨胀因子 1 + (k-1)*rho = 2.72
130 笔池化交易   ->  ≈ 48 个独立观测
```

这正是 §三.2 那条 `n_eff` 公式，只是用在**标的间**而不是时间自相关上 ——
仓库此前只做了时间维度。

#### 一个必须分开的定义

「有效样本」有两个互为倒数的量，混用会差 `(k-1)*rho_bar` 倍：

| 名称 | 公式 | 三主流 | 52 永续 | 用途 |
|---|---|---|---|---|
| `sample_inflation` | `1 + (k-1)*rho_bar` | **2.72** | 23.35 | 「130 笔其实是多少笔」 |
| `effective_bets` | `k / (1 + (k-1)*rho_bar)` | **1.10** | 2.23 | 「这真是 52 个赌注吗」 |

还有一个流通量：相关矩阵特征值的**参与率**（数的是因子个数，不是序列个数），
同一面板给 1.21 / 2.23 —— 数字更大。它需要线性代数依赖（本项目是纯标准库）。
**所以一个不带定义的「N_eff」不是可用主张** —— 这正是 §三.5「六次失误」的模式。

#### 关键区分：高相关伤的是时序，不是横截面

这一点不能读反：

| 用途 | 高相关的影响 |
|---|---|
| **时序 / 方向性**（现有 `engine.Backtester`） | **致命** —— 池化 n 被高估 2.7 倍 |
| **横截面 / 相对**（`cross_section.py`） | **无害，反而是功效来源** —— 共同因子被构造性消掉，信号测的是相对排名 |
| **组合集中度** | 有害 —— 5 个 0.9 相关的仓位是一个仓位 |

所以**「做相关性过滤」和「提升统计功效」是两件事**：过滤改变的是**交易哪些标的**，
不改变**有多少独立观测**。后者只能靠横截面（已验证）或延长时间来改善。

#### 顺带发现：归档把现货对与永续混在一起

初测时选池里混进了 `@142`、`@151`、`@107` 这类名字 —— 那是 Hyperliquid 的
**现货对**（`@index` 命名），价格量级 `2e-07`，**本系统根本无法交易**
（bot 的票池来自永续 `meta` 端点）。

实测占比：**117 / 882 个名字**，占美元成交额 **1.8%**，但**前 20 名里有 4 个**。
（先说成「主导成交额」是过头了，实测只有 1.8%。）所以任何 top-N 选池都会被
它们混入，而 `2e-07` 价格上的「收益率」是两个舍入误差之比，其相关性是噪声。

已加 `universe.is_perp_name` / `tradeable_names` 过滤。过滤后平均相关从 +0.479
降到 +0.438，ρ≥0.8 的分组从 30 变成 47（最大组 5：AVAX, BTC, ETH, LINK, SOL）。

#### 实现

`backtest/universe.py`：相关性、两种样本修正、传递闭包聚类、按流动性一簇取一个。
纯标准库，离线可测（40 条测试）。阈值实测：ρ≥0.9 几乎不合并（52 组），
ρ≥0.8 合到 47 组，**ρ≥0.7 才真正合并到 35 组** —— 所以 0.7 是有意义的工作点。

---

### 8. 分散票池上的方向性回测：把统计功效提上去了，效应没有出现

§二.7 的直接后果：既然 3 个标的只有 1.10 个有效注数，那就换一个真正分散的票池再跑。

**口径**（与基线只差票池一个变量）：

| | 值 |
|---|---|
| 票池 | 成交额前 60 → 过 `tradeable_names`（-8）→ 覆盖 ≥95%（-4）→ ρ≥0.7 聚类去重 → **31 个** |
| 池内平均两两相关 | **+0.365**（原始 52 个是 +0.438，选池确实降低了相关） |
| `effective_bets` | 2.59 |
| `sample_inflation` | 11.96 |
| 窗口 / 配置 | `--as-of 2026-09-15` × 170 天，balanced |

**结果**：

| | 3 个主流标的 | **31 个分散标的** |
|---|---|---|
| 交易 | 175 | **2121**（12 倍） |
| **有效独立观测** | 175 / 2.72 = **64** | 2121 / 11.96 = **177**（2.8 倍） |
| profit factor | 1.22 | **1.10** |
| 胜率 | 24.0% | 30.6% |
| 期望/笔 | $+0.27 | $+0.23 |
| 总盈亏 | +$47.52 | +$486.42 |
| PF>1 的市场 | 2 / 3 | **17 / 31** |

**关键判据（多重比较）**：31 个市场里 **11 个**超过随机入场第 75 百分位。
纯随机下期望是 **7.75 个**，`P(X ≥ 11) = 0.128` —— **不显著**（5% 门槛需要 ≥13 个）。
同时低于 p25 的有 9 个（期望 7.75）。

**所以：扩大票池把有效观测从 64 提到 177，削弱了噪声，却让「优势」变得更小
（PF 1.22 → 1.10）而不是更清楚。** 这正是「3 标的 PF 1.32」那个数字的来源
—— 它建立在大约 48 个独立观测上，本来就不该被当作证据。

#### 我的预测错了，方向是偏悲观

跑之前我按 `ρ̄=0.438 / k=35` 预测有效观测**只从 48 提到约 63**。实测是
**64 → 177**，我低了约 2.8 倍。原因有两个，都值得记下来：

1. **选池本身降低了相关**：0.438 是原始 52 个标的的 ρ̄，而 ρ≥0.7 去重后的 31 个
   是 **0.365**。我把没经过选池的数字套在了选池后的池子上。
2. **交易数是 12 倍不是 10 倍**：31/3 = 10.3，但分散票池的每标的交易数更高。

教训和 §三.5 同一条：**预测要写在跑之前，跑完要对照，错了要说。** 如果我只是
事后报一个「177」，这个预测偏悲观的事实就消失了。

#### 保留

- 票池从 35 收到 31，是因为 4 个标的（CHIP/TON/CASHCAT/PONS，覆盖 8.9%~74.5%）
  的窗口不是 170 天。**这件事本身值得记**：只按流动性选池一定会混入新上市标的。
- funding 在部分市场只覆盖约 190/200 天，未覆盖的日子按零资金费计 —— **美化多头**。
- 31 个市场 × 1 个窗口仍是 1 个观测。**不重叠窗口重复仍没做**（§五）。
- 买入持有在多数市场碾压策略（ARB +9.76% vs +71.87%；BTC +2.79% vs +14.61%），
  唯一的例外是 BCH（策略 +6.70% vs 买入持有 **−52.12%**）。

---

### 9. 四项纯趋势硬门槛：独立可用吗？——不

提议：只用四个二值趋势条件做硬门槛，每根 1h bar 给 0–4 票多头共识，
`min_bull_votes` 为入场阈值。四个条件全部只用过去数据：24h 涨幅 > 0、
量能高于自身 20 日均值、收盘站上 SMA20、EMA21 > EMA55。

实现在 `backtest/trend_gate.py`（纯函数 + 明确成本模型 + 配对敞口的随机对照）。

**结果**（`--as-of 2026-09-15` × 170 天，出场 = 票数跌破阈值，无额外参数）：

| 阈值 | 标的 | 交易 | 敞口 | 净收益 | 买入持有 | 随机中位 | 百分位 |
|---|---|---|---|---|---|---|---|
| 2 | BTC | 245 | 59.7% | **−13.05%** | +14.61% | −9.76% | 37 |
| 2 | ETH | 269 | 59.6% | **−3.31%** | +20.85% | +6.80% | 20 |
| 2 | SOL | 230 | 57.8% | +5.93% | +19.00% | +3.51% | 53 |
| 3 | BTC | 228 | 36.1% | **−6.81%** | +14.61% | −8.56% | 57 |
| 3 | ETH | 227 | 34.8% | **−18.25%** | +20.85% | +0.23% | 13 |
| 3 | SOL | 232 | 36.3% | **−7.54%** | +19.00% | −0.86% | 37 |
| 4 | BTC | 191 | 11.2% | **−9.52%** | +14.61% | −10.05% | 60 |
| 4 | ETH | 190 | 10.9% | **−11.39%** | +20.85% | −7.34% | 37 |
| 4 | SOL | 169 | 12.0% | +6.45% | +19.00% | −5.71% | 77 |

**9 格中 7 格亏钱；9 格中 0 格跑赢买入持有；超过随机第 75 百分位的 1 格**
（纯随机期望 2.25 格，**低于期望**）。

结论：**不能形成独立可用的入场规则。** 而且失败方式很具体 —— 不是"没有优势"，
是「敞口 36–60% 的择时把买入持有的涨幅让掉了，同时付了 230–270 次往返的手续费」。

#### 一行时点差把结论从「满分」变成「亏钱」

第一版 `simulate` 的出场按 `opens[i]` 成交，而触发出场的票数用的是 `closes[i]`。
**`opens[i]` 在时间上早于 `closes[i]`** —— 等于用还没发生的收盘价决定出场。
入场当时已正确延迟到 `opens[i+1]`，出场漏了。

| | 出场时点 | BTC 阈2 | ETH 阈2 | SOL 阈2 | 9 格百分位 |
|---|---|---|---|---|---|
| 有前视 | `opens[i]` | +49.77% | +59.04% | +108.62% | **9/9 全是 100** |
| 修正后 | `opens[i+1]` | −13.05% | −3.31% | +5.93% | 1/9 超 p75 |

**同一份策略，一行之差，从「全部满分」变成「亏钱」。** 这是 §三.5 记录的
同一类失误的第 7 次。

#### 测试也漏了一轮，而且是两层

修完之后写了 `TestNoLookAhead`，**变异验证显示它对前视偏差捕获 0 个**。两个原因：

1. **断言测的是自洽性，不是关系。** 我写的是 `exit_price == opens[exit_bar]` ——
   把成交挪到前一根时，**这两个字段一起移动**，仍然自洽，所以断言照过。
   修正：`Trade` 增加 `entry_signal_bar` / `exit_signal_bar`（记录产生决策的 bar），
   断言改为 **`fill_bar > signal_bar`**。这个性质此前无法表达，因为
   Trade 里根本没有「触发 bar」这个字段。
2. **测试序列从不走正常出场路径。** 单调上涨序列只产生 1 笔交易，而它是
   **收尾平仓**（不走 `close_trade`）。所以「出场退回一根」这个变异对它完全无效。
   修正：加一段**涨—跌—涨**的序列，并断言「至少有一笔在样本结束前平仓」，
   否则测试等于没测。

修完后 M1/M2 都能被捕获（1 个 / 2 个测试）。**这一条比结论本身更值得记**：
「测试通过」和「测试承重」是两件事，而后者只能靠变异验证。

#### 另外两处口径

- **`hold_bars` 名不符实**：因为 1 根成交延迟，「3 根」实际持仓 4 根。已改为
  `>= hold_bars - 1`，让参数名副其实（发现方式：固定持仓测试断言 ≤3 而得到 4）。
- **随机对照不得与真实规则同 bar 换手**：真实规则在 `opens[j]` 出场后，
  下一笔最早只能 `opens[j+1]` 成交。对照原先允许 `entry == 上一笔 exit`，
  **比规则多一根敞口** —— 这是偏向对照的偏差，也就是对被测规则不利的方向。
  已改为 `cursor = exit_bar + 1`，并把「不重叠」拆成 `random_spans` 直接断言
  （用收益阈值抓不住：重叠的对照照样给出像样的分布，实测捕获 0 个）。

---

### 10. 那个真正的逻辑问题：手续费，而不是行情

提问：**2026 行情不错（BTC +14.61% / ETH +20.85% / SOL +19.00%），为什么所有
指标回测都这么差？** 这个质疑是对的，而且查出来的东西跨越两个策略。

#### 拆开：毛收益 vs 净收益

同窗口、同参数，只把手续费设为零：

| 策略 | 交易 | 毛收益 | 净收益 | **手续费拖累** |
|---|---|---|---|---|
| 四项趋势门槛（阈3，对称出场）BTC | 228 | +9.32% | −6.81% | **16.13pp** |
| 同上 ETH | 227 | −4.17% | −18.25% | **14.08pp** |
| 同上 SOL | 232 | +8.77% | −7.54% | **16.31pp** |
| **现有主策略（balanced，3 标的）** | 175 | PF **1.29** / +$60.04 | PF **1.22** / +$47.52 | **12.52pp** |

**两条独立的路径都指向同一件事：`交易数 × 2 腿 × 3.5bp` 是数量级 12–16pp 的
成本项，与毛收益同阶甚至更大。** 而仓库此前**所有回测都只报净值** ——
这个数字从来没被单独看见过。

具体到主策略：**毛 PF 1.29，净 PF 1.22**。也就是说超过盈亏平衡的那部分优势里，
有相当一块是**被手续费吃掉的**，而不是策略本身没有。

#### 换手率是被约束项，不是信号

对称出场（票数跌破阈值即平）的问题不是「信号错」，是**它每 18 小时换一次手**。
把出场改成滞后（只在四票全灭时平仓）：

| 出场规则 | 交易 | 均持仓 | 成本拖累 | BTC 净 | ETH 净 | SOL 净 |
|---|---|---|---|---|---|---|
| 对称（≤2 出） | 228 | 6.5 | 16pp | −6.81% | −18.25% | −7.54% |
| 滞后（≤1 出） | ~108 | 18.0 | 8pp | +4.57% | −6.66% | +11.09% |
| **滞后（≤0 出）** | **49** | **52.6** | **4pp** | **+15.50%** | −2.24% | **+32.71%** |

**交易数降 4.6 倍，成本从 16pp 降到 4pp，净值由负转正。**

#### 但「变好」的机制是更像买入持有，不是更好的择时

同一批交易换成随机入场（持仓时长取自真实交易，所以敞口配对）：

| 标的 | 敞口 | 策略 | 随机中位 | **随机 p95** | 百分位 |
|---|---|---|---|---|---|
| BTC | 63.2% | +15.50% | +9.00% | **+43.14%** | 63 |
| ETH | 62.5% | −2.24% | +16.66% | **+62.61%** | 23 |
| SOL | 60.7% | +32.71% | +7.23% | **+36.65%** | 90 |

**1/3 超 p75（期望 0.75），不显著。** 而**随机中位本身是 +9.00% / +16.66% /
+7.23%** —— 在上涨行情里，随机入场、持同样的时长也一样赚钱。策略的择时在
这个分布里没有区分度。

**所以正确的读法是：滞后阈值把「择时失败 + 换手过高」改成了「更接近长期持有」，
收益来自降低换手，不是来自预测。** 这也解释了为什么毛收益（9.32% / −4.17% /
8.77%）在 36% 敞口下并不荒谬，却仍然跑输 100% 敞口的买入持有。

#### 每个条件单独看：三个标的不一致

前瞻 24h 收益（bp），条件为真 / 为假：

| 条件 | BTC | ETH | SOL |
|---|---|---|---|
| momentum | **+17.5 / +1.3** | +11.2 / +18.7 | +10.4 / +19.9 |
| volume | +6.1 / +11.8 | +4.1 / +19.9 | +14.8 / +15.0 |
| bollinger | **+12.1 / +7.5** | +10.7 / +19.1 | **+17.4 / +12.3** |
| ema | +6.1 / +14.3 | +3.8 / +26.4 | **+17.4 / +12.3** |

**momentum 只在 BTC 有效、EMA 只在 SOL 有效、volume 三个都不行。** 一个条件在
三个高度相关的标的上给出三种答案，这是噪声的特征。而且方向与 §二.7 一致 ——
仓库测到的唯一效应是**短周期反转**（IC −0.041 @24h），而趋势门槛押的是延续。

#### 由此产生的两条通用要求

1. **每次回测必须同时报毛收益与净收益。** 只报净值等于在报手续费表。
   净值把「毛优势被成本吃掉」和「本来就没有优势」混成同一个数字。
2. **收益必须按敞口归一化。** 63% 敞口的 +15.50% 与 100% 敞口的 +14.61% 不是
   同一个主张；随机对照把敞口配平之后，前者立刻失去显著性。

---

### 11. 回调入场：目前唯一值得继续做的规则

提议（偏做多）：

```
momentum   close[i] / close[i-24] - 1 <= 3%     （不追高）
cross      EMA(21) 上穿 SMA(20)                  （收复中线）
volume     volume[i] > mean(volume[i-480..i])    （放量）
```

三条同时成立才入场；持仓期间价格跌破中线即离场。实现在 `backtest/pullback_entry.py`。

**方向变了，而且这次是对的。** §二.9 的四票门槛是**延续**规则（买强势），而仓库唯一
测到的效应是 24h **反转**（横截面 IC −0.041 @24h，t = −4.1）。把「24h 必须为正」
换成「24h ≤ 3%」，等于把赌注挪到有效应的那一侧。

#### 结果：全窗口

| 读法 | 标的 | 交易 | 敞口 | 毛 | **净** | 买入持有 | 百分位 |
|---|---|---|---|---|---|---|---|
| 事件穿越 | BTC | 46 | 16.7% | +7.81% | +4.39% | +14.61% | 70 |
| 事件穿越 | **ETH** | 45 | **19.8%** | +33.71% | **+29.56%** | +20.85% | **100** |
| 事件穿越 | SOL | 52 | 19.0% | +12.33% | +8.32% | +19.00% | 75 |
| 状态（在中线之上） | BTC | 108 | 36.1% | +2.16% | −5.28% | +14.61% | 55 |
| 状态 | ETH | 101 | 38.9% | +28.97% | +20.16% | +20.85% | 85 |
| 状态 | SOL | 107 | 38.8% | +16.61% | +8.20% | +19.00% | 65 |

**ETH 用 19.8% 的敞口赚到 +29.56%，超过买入持有（+20.85%），且在 20 次配对随机
对照里排第 100 百分位。** 成本也不再是主导项（45 笔 × 7bp ≈ 3.2pp，占毛收益约 9%）
—— 也就是说，**这条规则通过了 §二.10 那个把前两条规则打死的换手率检验**。

#### 分段稳定性：7/18，不显著，但好于此前一切

6 段不重叠 × 3 标的（18 格）：**7 格超过 p75，纯随机期望 4.5**，
`P(X ≥ 7) ≈ 0.09`。**仍不显著**，但对比：四票门槛是 1/9（低于期望），
横截面分段是 3/6（p≈0.17）。

逐段符号仍然不稳（BTC：25/100/70/90/**0**/95）。ETH 全窗口的 +29.56% 主要来自
第 1、6 段（+10.56%, +21.18%），中间四段为负。

#### 但收益是脆的

| 标的 | 交易 | 胜率 | 最好一笔 | **去掉最好** | **去掉前二** |
|---|---|---|---|---|---|
| BTC | 46 | 39.1% | +5.92% | **−1.45%** | −6.00% |
| ETH | 45 | **57.8%** | +17.81% | **+9.97%** | +3.29% |
| SOL | 52 | 44.2% | +9.90% | **−1.44%** | −7.94% |

**BTC 和 SOL 的全部收益来自单笔最佳交易。** 只有 **ETH** 承受得住去掉最好一笔
（+9.97%），而且它的胜率 57.8% 是唯一像样的分布特征。

**所以正确的结论是：这条规则值得继续做，但依据目前只有 ETH 一个标的、
一个窗口。** 不是「找到了策略」。

#### 顺带修正：3% 上限几乎不筛东西

| 标的 | EMA>中线 | 24h≤3% | **两者同时** | +量能 | 上穿+量能 |
|---|---|---|---|---|---|
| BTC | 50.4% | **93.4%** | 46.4% | 13.4% | 1.00%（95 次） |
| ETH | 50.9% | 87.9% | 43.5% | 12.0% | 0.96%（91 次） |
| SOL | 50.0% | 84.4% | 40.4% | 11.2% | 0.96%（91 次） |

**「24h ≤ 3%」在全体 bar 上已有 93.4% 满足**，在 EMA 之上时是 92.2% ——
**两个条件近乎独立**，上限只筛掉 6.6%。真正在筛的是**量能**（50% → 12–13%）。

我此前推断「EMA 上穿中线需要近期急涨，因此必然与 24h ≤ 3% 互斥」，
**这个推断是错的**，被上表否掉。这是本会话第三次「先推理后测量、测量推翻推理」，
而三次都是测量才对。

#### 一个关于该条件的事实

**线性上涨序列上 EMA(21) 永远低于 SMA(20)**（700 根测试，0 根满足）——
因为指数权重等效滞后 10 根，而简单均值是 9.5 根。所以「EMA 上穿中线」**不是
趋势过滤器**，它需要近 20 根内的相对强势。这条事实一开始是测试夹具的障碍，
后来发现它本身就是关于该条件的说明，已写进 `tests/test_pullback_entry.py`
并有独立断言。

#### 实现说明

复用 `trend_gate.simulate_flags`（§二.9 那个经过变异验证的成交循环），
没有为这条规则写第二套模拟器 —— 那个循环里的成交时点保证曾经错一次，
而错的那一次把亏损规则变成了「9/9 满分」。

覆盖：27 条测试，6 项变异中有 5 项被捕获（第 6 项 `hold` 恒为 True 起初
捕获 0 个，补了 `TestExitConditionActuallyBites` 后捕获 2 个）。

---

### 12. EMA9/26 金叉变体：头部数字更好，但证据更差

提议改成：**涨幅 0%~3%**（现在有下界）+ **EMA9 上穿 EMA26**。量能条件未提。

#### 头部结果

| 变体 | BTC | ETH | SOL |
|---|---|---|---|
| EMA9/26 + 0~3% **+量能** | −1.14%（p50, 31 笔） | **+35.90%**（p100, 27 笔） | +6.98%（p50, 18 笔） |
| EMA9/26 + 0~3% **无量能** | **+20.20%**（p85, 64 笔） | +15.43%（p70, 72 笔） | +2.64%（p30, 51 笔） |
| EMA9/26 + **≤3% 无下界** 无量能 | +11.59%（p70, 80 笔） | **−2.49%**（p45, 93 笔） | +19.72%（p75, 72 笔） |

两个副产品：**量能在这里是拖累**（BTC 从 +20.20% 掉到 −1.14%），
和 §二.11 相反；**0% 下界在这里有用**（去掉后 ETH 从 +15.43% 变 −2.49%），
而 3% 上限仍然几乎无效。

#### 但分段证据从「好于一切」掉到「等于随机」

6 段不重叠 × 3 标的（414 天）的百分位：

```
+量能   BTC: 15  0 35 35 80 45    ETH: 45 20 45 70 85 95    SOL:  -  5 60 45 60 85
无量能  BTC: 25 10 40 65 90 60    ETH: 25 10 15 60 55 85    SOL: 70 15 75 45 10 80
```

| 规则 | 超 p75 | 期望 | 结论 |
|---|---|---|---|
| §二.11 EMA21 vs 中线，≤3%，无下界 | **7/18** | 4.5 | p ≈ 0.09 |
| EMA9/26 + 0~3% +量能 | **4/17** | 4.25 | **等于随机** |
| EMA9/26 + 0~3% 无量能 | **4/18** | 4.5 | **等于随机** |

**改这一处把规则从「目前唯一好于随机」变成「完全等于随机」**，而头部 ETH 的数字
反而更好看。原因是结构性的，见下。

#### 为什么：这两个「金叉」不是同一类条件

| 条件 | 线性上涨斜坡上的表现 | 性质 |
|---|---|---|
| `EMA21 > SMA20` | **700 根里 0 根满足** | 加速度（需近 20 根相对强势） |
| `EMA9 > EMA26` | **700 根里 660+ 根满足** | 趋势（两者都有无限记忆，滞后 4 vs 12.5） |

指数权重滞后 10 根而简单均值滞后 9.5 根 —— 所以 `EMA21 > SMA20` 在平稳上涨中
恒不成立；而两条 EMA 相比时，快线上穿慢线正是**趋势确认**。

**于是这一改把规则从「反转那一侧」挪回了「延续那一侧」** —— 而仓库唯一测到的
效应是 24h **反转**（§二.7）。分段证据变差与这个方向变化一致。

#### 收益全是单笔

| 变体 | 标的 | 交易 | 胜率 | 最好一笔 | **去最好** | **去前二** |
|---|---|---|---|---|---|---|
| +量能 | BTC | 31 | 38.7% | +5.36% | **−6.17%** | −8.15% |
| +量能 | ETH | 27 | 37.0% | **+26.17%** | +7.71% | **−2.13%** |
| 无量能 | ETH | 72 | 29.2% | **+26.17%** | **−8.52%** | −16.88% |
| 无量能 | SOL | 51 | 25.5% | +22.35% | **−16.11%** | −21.88% |

**ETH 在两个变体里的「最好一笔」都是 +26.17% —— 是同一笔交易。** 头部那个
+35.90% 就是它加上运气；去掉它只剩 +7.71%，去掉前二变负。

#### 一个必须点出的模式

**ETH 是用户提出的每一版规则里唯一的亮点**（§二.11 的 +29.56%、这里的 +35.90%）。
在三个高相关标的（ρ̄ = 0.859）上，只有一个标的、而且只在部分变体里突出 ——
这更像**这个标的在这个窗口的属性**，不是规则的属性。

要区分这两者，需要的是把规则放到 §二.7 的分散票池（31 个标的）上，
而不是继续在 BTC/ETH/SOL 上调参数。**这一点在 §六 已经列为下一步，现在更紧急。**

#### 实现

`PullbackConfig` 增加 `cross_reference`（`"sma"` / `"ema"`）、`ema_slow_period`、
`require_volume`，并提供 `ema_pair()` 具名构造器。**默认值未改**（EMA21 / sma /
无下界 / 需量能），所以 §二.11 已报出的每个数字仍可复现 ——
把 9/26 设成默认会让那些数字无声地失去依据。

覆盖：37 条测试，其中 `TestTheTwoCrossReadingsDifferInKind` 用线性斜坡
把两类条件的区别固定下来（一个 660+ 根满足，一个 0 根）。

---

### 13. 从一张图反推的规则：罕见与可测是冲突的

提供了一张图，问「这种情况下买入」的具体指标与策略。图面读数：

| 项 | 数值 |
|---|---|
| 布林带 (20, 2σ, SMA) | 中轨 1476.40 / 上轨 1484.88 / 下轨 1467.92 |
| EMA9 | **1474.83** |
| EMA26 | **1482.63** |
| 价格 | ≈1476（贴着中轨） |

**第一个发现是规格自相矛盾**：EMA9（1474.83）**低于** EMA26（1482.63），
所以图上右端是**死叉状态**，金叉尚未发生。若规则是「EMA9/26 金叉入场」，
此刻的动作是**等**；买在这里是**提前埋伏**，那是另一条规则。

图面形态：脉冲上涨 → 高位横盘 → 回落到均线区 → 右端早期转折。所以
可测的形式是**「脉冲 + 回踩 + 金叉」**，即在金叉上再加两个上下文条件。
已实现为 `PullbackConfig.impulse_lookback / min_impulse / min_pullback /
max_pullback`（默认关闭，已有数字不受影响）。

#### 测量：上下文过滤让结果更差，而且把样本毁掉

| 变体 | BTC | ETH | SOL |
|---|---|---|---|
| 无上下文（裸金叉） | **+20.20%**（p85, 64笔） | **+15.43%**（p70, 72笔） | +2.64%（p30, 51笔） |
| 脉冲≥3% 回踩1~5% | +9.23%（p75, 19笔） | +4.64%（p60, 30笔） | −4.43%（p25, 21笔） |
| 脉冲≥5% 回踩1~5% | +1.64%（p75, **2笔**） | +1.13%（p70, **5笔**） | −0.07%（p45, 12笔） |
| 脉冲≥5% 回踩1~3% | −0.50%（**1笔**） | −1.45%（**2笔**） | −2.22%（7笔） |

#### 为什么：频率才是瓶颈

170 天交易窗口内的出现次数：

| 标的 | bar数 | 脉冲≥5% | EMA9>26 | **金叉** | **冲5%+踩+叉** |
|---|---|---|---|---|---|
| BTC | 4080 | 652 | 2119 | **80** | **5** |
| ETH | 4080 | 1473 | 2102 | **95** | **11** |
| SOL | 4080 | 1890 | 2132 | **74** | **20** |

**金叉本身就少（74–95 次/170 天）**，再要求它同时落在「脉冲后回踩 1~5%」里
就只剩 **5–20 次**——扣除被 0~3% 上限和持仓状态挡掉的，实测成交 2–12 笔。

**图上的形态之所以一眼可见，正是因为它罕见。罕见与可测是冲突的**：
一个每 85 天才出现一次的条件，在 170 天的样本里最多给两次观测。

#### 结论与出路

- 这个具体形态**在这份数据上无法评估**（3 标的 × 170 天）。不是「效果不好」，
  是**没有样本**。
- 若要评估，唯一办法是**扩大标的数**（§二.7 的 31 个分散票池 ≈ 10 倍事件数，
  即 150–600 次），而不是延长时间（数据已取尽，§五）。
- **在此之前不要根据这张图下任何结论。** 它是用户按结果挑出的一个实例，
  这正是 §三.1 那条「随机对照是唯一有效判据」要防的东西。

#### 一个副产品：我的夹具连续错了四次

写 `TestImpulseAndPullbackContext` 时，夹具四次失败，每次原因都不同且都是
真实事实：长度差 1；完全平的序列上 EMA9 == EMA26；陡涨使 24h 变化达 72% 被
3% 上限挡掉；**默认参照是 EMA21/SMA20，而稳定上涨中 EMA21 < SMA20**——
最后一条正是 §二.12 记录的结构性事实。四条都写进了测试的文档字符串。

---

### 14. AAVE 15m：按图识别的形态，在上涨行情里毛收益已经为负

第二张图给出的是 **AAVE · 15 分钟 · Hyperliquid**，并明确说「不要被前面的描述误导」，
即让我自己识别形态。图面读数：

| 项 | 数值 |
|---|---|
| 最后一根 | 开 115.64 / 高 **116.87** / 低 115.36 / 收 **116.87**（**收在最高**，+1.10%） |
| 布林带 (20,2σ) | 中轨 115.945 / 上轨 117.847 / 下轨 114.043 |
| EMA9 / EMA26 | 115.4907 / **116.4720**（**EMA9 仍低于 EMA26**） |
| MACD | 线 0.1412 / 信号 **−0.8616**（**线在信号之上**） |

**两张图的共同状态就是这个，而不是 EMA 交叉：**

```
① 价格收复布林中轨（收在中轨之上）
② 但 EMA9 仍低于 EMA26   ← 金叉尚未发生
③ MACD 线已上穿信号线     ← 比 EMA 交叉更早的确认
```

所以真正的形态是**「价格先收复中轨、均线还没金叉」这个早期状态**，
赌金叉随后出现。等 EMA9/26 真金叉，价格已经涨过一截。已实现为
`cross_subject="price"`（价格而非 EMA 作为穿越主体）、`require_lagging_cross`、
`require_macd`，默认全部关闭。

顺带解开了两张图的 MACD 显示顺序：**第三个值是 `信号 − 线`**，不是柱值本身
（图一 `−8.6885 = −7.0402 − 1.6484`，图二 `−1.0028 = −0.8616 − 0.1412`）。

#### 测试条件

AAVE 在归档里完整可得（324 万根 1s bar，414 天，价格 57.7–386.4），15m 直接可测。

**第一次对照失败，而失败本身是发现**：我选了 6 个标的想找一个上涨的做对照，
结果**全部下跌**（BTC −33.0%、ETH −32.7%、SOL −38.1%、AAVE −50.1%、DOGE −59.2%、
LINK −30.6%）。所以那个试验无法区分「形态不行」和「市场不行」。

#### 在上涨窗口中重做

最后 170 天（15m）：

| 标的 | 买入持有 | 交易 | 敞口 | **毛** | 净 | 成本 | **百分位** |
|---|---|---|---|---|---|---|---|
| BTC | +14.6% | 133 | 7.5% | **−1.8%** | −10.5% | 8.7pp | 35 |
| ETH | +20.9% | 136 | 7.0% | **−7.4%** | −15.8% | 8.4pp | 15 |
| SOL | +19.0% | 140 | 6.8% | **−17.0%** | −24.8% | 7.7pp | **0** |
| AAVE | +28.1% | 151 | 7.3% | **+0.3%** | −9.7% | 10.1pp | 55 |
| LINK | +29.3% | 128 | 6.9% | **−5.7%** | −13.8% | 8.1pp | 20 |

**0/6 超过 p75（期望 1.5），六个全为负，而且五个里四个的毛收益已经为负。**

**所以这次不是成本问题**（§二.10 的趋势门槛是毛 +9% 被 16pp 成本吃掉），
**是信号本身没有毛优势** —— 在上涨 15–29% 的行情里、只用了 7% 敞口，
毛收益仍是 −1.8% ~ −17.0%。均持仓 8.5 根（约 2 小时），是准剃头式的高换手。

#### 加上「下跌后」上下文则无法测

| 变体 | 414 天成交 | 百分位 |
|---|---|---|
| 前 24h 内跌过 ≥5% | **9 笔** | 15 |
| 前 24h 内跌过 ≥3% | 82 笔 | 20 |
| 前 4h 内跌过 ≥2% | **1 笔** | 10 |

分段成交数 **2 / 0 / 0 / 0 / 1 / 6** —— 等于没有样本。

#### 结论

**这是同一模式的第三次出现**（§二.13 的 5–20 次/170 天、这里的 1–9 次/414 天）：
**用户按结果挑出的那个实例看着很好，而把它写成规则之后，在总体上是负的。**

第三次的结论比前两次更硬，因为这次做了正确的对照（上涨窗口），
而且分解了毛/净（排除了成本解释）：**这个形态没有可测的毛优势。**

---

### 15. 均值回归（RSI / Keltner）：本仓库第一个毛收益为正的规则

转向均值回归。这不是又一次盲试 —— §二.7 测到的**唯一**通过全部检验的效应
就是反转（IC −0.041 @24h，t = −4.1 ~ −6.1）。但必须区分两种形态，因为结果不同：

| | 做法 | 共同市场因子 | 本仓库证据 |
|---|---|---|---|
| **横截面** | 买**相对最弱**的、空相对最强的 | **被构造性消掉** | **有效应** |
| **时序** | RSI 超卖 / 跌破 Keltner 下轨 → 买 | **就是全部仓位** | §二.1–6、8 全部无据 |

RSI 与 Keltner 的经典用法是**时序**的，所以先做时序版（`backtest/mean_reversion.py`），
横截面版另行测（未做，见 §六）。

#### 口径

45 个标的（成交额前 45 的非现货、覆盖 ≥95%），1h，170 天
（`--as-of 2026-09-15` 窗口，买入持有中位 +12.4%）。复用 §二.9 那个经过变异
验证的成交循环 `simulate_flags`。

#### 结果：毛收益为正，与之前所有规则有本质区别

| 规则 | 交易 | 敞口 | **毛合计** | **净合计** | 净>0 | 超 p75 | 期望 |
|---|---|---|---|---|---|---|---|
| **RSI14 ≤30 入 / ≥50 出** | 1430 | 15.7% | **+446.4%** | **+338.4%** | **26/45** | 14/45 | 11.2 |
| Keltner EMA20−2×ATR10 | 2208 | 18.5% | +427.9% | +261.8% | 23/45 | 13/45 | 11.2 |

对比：趋势门槛的毛收益 ≈ 0 甚至为负（§二.9/§二.13）。**这次信号本身有信息量。**

**分段稳定性（6 段不重叠 × 45 标的）**：

```
上涨窗口: 112/216 = 51.9% 超 p75   (期望 54.0, 原始 P=0.000, 修正 P=0.0091)
下跌窗口:  74/231 = 32.0% 超 p75   (期望 57.8, 原始 P=0.010, 修正 P≈0.26)
```

**修正是必要的**：216 个格子不独立（同标的跨段、段内共享市场因子）。按段内
`effective_bets = 45/(1+44×0.40) ≈ 2.4` × 6 段 ≈ 14.5 个有效独立格子折算后，
上涨窗口 z = 2.36（仍显著），下跌窗口 z = 0.63（**不显著**）。

#### ADX 趋势过滤：中位数翻倍

经验上均值回归在趋势里失效，ADX 是标准判别量。已做成可配开关并单独测：

| 变体 | 交易 | 敞口 | 净合计 | **中位** | **超 p75** |
|---|---|---|---|---|---|
| 无过滤 | 1430 | 15.7% | +338.4% | +4.4% | 14/45 |
| ADX<25 | 803 | 8.9% | +180.4% | +4.6% | 15/45 |
| ADX<20 | 372 | 4.3% | +49.3% | +1.7% | 10/45 |
| **ADX<30** | 1105 | 12.1% | +303.3% | **+9.1%** | **18/45** |

**ADX<30 是工作点**：中位数从 +4.4% 翻到 +9.1%，超 p75 从 14 升到 18
（期望 11.25，z=2.33）。分段 80/158 = 50.6%，修正 P=0.0121。
ADX<20 太紧（敞口只剩 4.3%，反而更差）。ADX 中位 25.2，ADX<30 覆盖 68% 的 bar。

#### 但必须同时说清的四个限定

1. **绝对收益低于买入持有**：中位 **+4.4%** vs 买入持有 **+12.4%**。它以 15.7% 的
   敞口跑赢「同等敞口的随机择时」，却跑不赢「一直持有」—— 它赢在**风险调整后**，
   不是绝对收益。
2. **尾部依赖很重**：毛合计 +338.4% → 去掉每标的**最好一笔**后 **+62.4%**。
   不过 3/45 由盈转亏，所以效应在符号上是广的，在幅度上靠尾部。
3. **下跌窗口修正后不显著**（P≈0.26）。那时它「少亏 42pp」**完全来自低敞口**，
   不是择时 —— 配对敞口的对照说得很清楚。
4. **成本吃掉毛收益的 24%**（+446 → +338），仍是重要项。

#### 我的预测错了，而且这次错在「偏悲观」

跑之前我写了 P1：时序均值回归**净收益为负、超 p75 低于期望**。实测净 **+338%**、
14/45 超 p75（高于期望 11.2）。**P1 是错的。**

这是本会话第二次预测失准，方向与 §二.8 相反（那次偏悲观 2.8 倍，这次又偏悲观）。
两次都记下来，因为**只报事后数字会让预测这件事消失**。

P2（成本是主因）部分成立：成本占毛收益 24%，重要但不是主导。
P3（横截面版更好）尚未测。

#### 实现

`backtest/mean_reversion.py`：RSI 与 Keltner 两套规则的布尔序列、Keltner
通道（EMA ± k×ATR）、可配 ADX 过滤，复用 `simulate_flags`。25 条离线测试，
7 项变异中 6 项被捕获（第 7 项是我的变异本身无效）。

---

### 16. 给均值回归加止损：左尾确实被截断，但效应测不出

§二.15 的三处弱点里最具体的一处：该规则**没有止损**，而它的已知死法是尾部
（去掉每标的最好一笔后，+338% 掉到 +62%）。所以下一步是加止损再测。

#### 落地

止损进的是**共用模拟核心** `trend_gate.simulate_flags`，不是另写一套循环 ——
遵循 §四那条约定（成交时点只有一份实现）。为此加了三个东西：

- `TrendGateConfig.stop_loss_pct`（默认 0 = 关闭）与 `stop_cooldown_bars`
- `Trade.exit_reason`（`hold` / `cap` / `stop` / `markout`）与 `Trade.stop_level`
- `simulate_flags(..., lows=)` —— 止损必须看**最低价**，用收盘价会漏掉它存在的意义

**向后兼容是硬要求**：止损关闭时，四个既有模块的结果必须逐位不变。实测确认
（RSI/BTC 仍是 34 笔 / +8.39% / 17.7% / 21.2 bar），因为共核一旦被静默改动，
之前所有结论都会失效。

#### 两个窗口给出相反答案

| 止损 | 上涨窗口（45 标的） | 下跌窗口（40 标的，买入持有 −60.3%） |
|---|---|---|
| **无** | **+303.3%** / 中位 +9.1% / 超p75 18/45 | **−236.4%** / 中位 −8.0% / 均亏损 −5.1% |
| 2% + 冷却 | −19.9% / −2.0% / 7/45 | **−28.5%** / **−0.8%** / **16/40** / 均亏损 **−2.5%** |
| 5% + 冷却 | +154.9% / +6.0% / 17/45 | −55.4% / −4.0% / 14/40 |
| 15% | — | **−511.1%** / −18.9% / 9/40 |

**上涨窗口止损大亏，下跌窗口止损大赚。** 左尾的截断是真实的、机械的
（均亏损 −5.1% → −2.5%，最差单笔 −33.5% → −24.7%），但在一个上涨的市场里
它把**超过一半的入场**（2% 止损下 54–58%）变成已实现的小亏。

这正是一直写在配置注释里的那个内部矛盾：**规则说「下跌是暂时的」，止损说
「跌这么多就信它」—— 两者不可能同时成立**。测量说规则对得更多。

#### 全周期看着有救，分段否定它

全归档 414 天连续序列（买入持有中位 **−57.7%**，仅 5/40 正收益）：

| 止损 | 冷却 | 敞口 | 净合计 | 中位 |
|---|---|---|---|---|
| 无 | — | 12.8% | +103.2% | −6.1% |
| 5% | 12 | 9.1% | +219.8% | −3.9% |
| **8%** | 0 | 10.7% | **+228.3%** | **−2.7%** |
| 12% | 0 | 11.7% | **−48.1%** | −8.6% |

8% 看起来比无止损好一倍。**但 12% 又掉到 −48%，这种非单调性是过拟合的信号**，
所以做了分段检验：

| | 段1 | 段2 | 段3 | 段4 | 段5 | 段6 |
|---|---|---|---|---|---|---|
| 无止损 | −194.6% | +119.0% | −288.8% | **−114.0%** | **+299.7%** | +140.4% |
| 2% | **+76.3%** | **+143.2%** | **−209.3%** | −171.2% | +151.5% | +8.6% |
| 12% | −221.2% | +58.3% | −343.1% | −137.7% | +282.0% | **+147.2%** |

**各段最优止损：2% | 2% | 2% | 无 | 无 | 12%** —— **8% 从未在任何一段胜出**。
3 段说止损有效、3 段说无效，**这就是「测不出效应」的样子**。全窗口那个
+228% 是求和出来的巧合，分段里不复现。

#### 结论：不采纳

**默认不启用止损。** 理由不是「止损不好」，而是**在这个样本上它的效应无法与
噪声区分**，而它在一个上涨市场里的代价是明确且大的。它是一项**有价格的保险**：
如果目标函数里有崩盘保护，那可以买；但这里测到的保费很高。

#### 顺带修掉的两件事

**1. 我引入了一个会让结果翻倍的 bug。** 改 `simulate_flags` 时把新的 mark-out
块插在了原有 mark-out **之前**，导致期末持仓被结算两次 —— 表现为多出一笔
`exit_reason=''` 的重复成交。**是一个既有测试抓到的**（它断言最后一笔的成交价
等于期末收盘价，而重复记录让「最后一笔」不再是真末笔）。如果只看收益数字，
会得到双倍暴露的虚假结果。

**2. 我的新测试一开始不承重。** 第一版 5 项变异只捕获 1 项：

| 变异 | 第一版 | 修正后 |
|---|---|---|
| 止损看收盘价而非最低价 | **漏** | 5 |
| 冷却被忽略 | **漏** | 4 |
| 止损位锚定信号 bar 收盘价 | **漏** | 3 |
| 退出原因不记录 | 3 | 5 |
| 止损延迟两 bar 成交 | **漏** | 5 |
| 止损填在止损价（隐藏跳空） | — | 3 |

三个漏掉的原因都是**夹具设计的问题，不是断言的问题**：
（a）用的下跌序列里收盘价也已跌破，两种实现都触发；（b）`assertLessEqual`
相等也通过，是真空断言；（c）夹具里 `opens == closes`，锚定哪个都一样。
改成**平坦价格 + 只有最低价刺穿 + 开盘价不同于收盘价**之后全部捕获。

教训与 §三.5 同源：**测试写完要变异验证；夹具的价格形状决定了它能区分什么。**

---

### 17. 横截面版 RSI：时序版不是它的劣化近似

§二.15 留的第二个问题：时序均值回归（RSI/Keltner）与仓库唯一测到效应的横截面
反转，**是不是同一个效应的两种写法**？如果是，时序版就该被横截面版取代。

#### 落地的两个横截面信号

`backtest/cross_section.py` 增加：

- `rsi_signal(period)` —— 同一份 `agent.indicators.rsi`，不是另写一套（否则两者
  不可比，而可比性是做这件事的全部理由）
- `stretch_signal(ema_period, vol_lookback)` —— `(close − EMA) / (σ·close)`，
  即「离自身均值的距离，以自身波动为单位」。这是 Keltner 下轨的横截面对应物
  （Panel 没有 high/low 列，用收益标准差代替 ATR 做尺度）

RSI 作为**排序键**有个结构问题：它有界，会**饱和**。实测单调序列直接钉在 0/100，
所以市场普跌时大半票池挤在 0 附近，排序在最该起作用的地方失去区分度。
`stretch` 正是为此而设：无界，且按自身波动归一化。

#### 一个必须说清的过程错误

第一次测在**成交额前 48 的流动标的**上，基线给出 **−33.9%（t=−0.98）**，
与时序版的正收益相反。我差点报成「仓库记录被推翻」。

实际是我读漏了 SPEC 自己的一句话：**效应完全在非流动那一半**（README 第 1158 行
「在流动的一半里什么都测不到（t ≈ −1.0）」），且记录流动子集是 **−55.88%**。
我的 −33.9% 正是**复现**那条对照。**换到正确的子集才有意义。**

#### 结果：三种信号同向，强度递减

非流动 86 标的（多空五分位，持有 24 根 1h，170 天）：

| 信号 | 均值/期 | t | 累计 | 随机 pct |
|---|---|---|---|---|
| **`negate(past_return(24))`（仓库基线）** | +0.204% | 1.28 | **+43.0%** | 100 |
| `negate(rsi_signal(14))` | +0.143% | 0.98 | +27.5% | 97 |
| `negate(stretch_signal(20,24))` | +0.122% | 0.89 | +22.6% | 97 |
| `past_return(24)` 正向（对照） | −0.312% | −1.95 | −49.4% | 3 |

流动 86（对照）：−9.8% / +4.9% / +8.1% —— **全部无效**。

**方向一致、位置一致（只在非流动一侧）、强度递减。** 所以横截面 RSI 是反转效应的
**一个较弱的形式**，不是新东西。

#### 但时序版不是它的近似 —— 2×2 给出了答案

| | 时序 RSI | 横截面 RSI |
|---|---|---|
| **非流动 86** | 超 p75 **48/86**（z=6.6） | +43.0%（pct 100） |
| **流动 86** | 超 p75 **35/86**（z=3.4） | **−9.8%（无效）** |

**时序版在两半都有效，横截面版只在非流动一半有效。** 若时序版只是横截面版的劣化
近似，它在横截面完全失效的地方（流动标的）就不该有信号 —— 但它有。

两者也不可比成一个数：时序版是**单边按敞口配对的择时**（对照物是同敞口的随机入场），
横截面版是**多空、市场因子被构造性消掉**。它们测的是不同的东西。

**共同点**是两侧都在非流动标的更强 —— 这符合直觉（小市值更均值回归），
但**也是微观结构假象最容易出现的地方**（陈旧报价的来回跳动会被读成均值回归）。

#### Keltner 带宽：常规默认值接近最优

非流动 86 标的，带宽扫描：

| 带宽 | 交易 | 敞口 | 净合计 | 中位 | 超 p75 | z |
|---|---|---|---|---|---|---|
| 1.0×ATR | 11646 | 37.3% | +852.7% | +2.2% | 56/86 | 8.6 |
| 1.5×ATR | 7705 | 28.7% | +1118.1% | +9.0% | 52/86 | 7.6 |
| **2.0×ATR（默认）** | 5006 | 20.8% | **+1127.9%** | **+11.4%** | 51/86 | 7.3 |
| 2.5×ATR | 3055 | 13.9% | +1097.8% | +11.7% | 49/86 | 6.8 |
| 3.0×ATR | 1569 | 7.6% | +823.8% | +8.9% | 47/86 | 6.4 |

**曲线平滑，不是尖峰** —— 这与 §二.16 止损扫描那种非单调形成对照，是「参数本身
合理」而非「拟合出了一个数」的证据。1.0×ATR 的 z 最高（8.6）但中位最差（+2.2%），
因为它交易最多而单笔边际最小；**常规的 2.0 在收益与中位上都最好**。

#### 抓到一个真 bug：`stretch` 的「EMA」曾经是简单均值

写测试时发现 `stretch_signal` 自称的 EMA 其实是 SMA：

```python
mid = ema(prices, ema_period)[-1]   # prices 恰好只有 ema_period 个
```

而 `agent.indicators.ema` 的种子是**前 `period` 个值的 SMA**，之后才递归。传进去
恰好 `period` 个值，递归一步都不跑，返回的就是那个种子 —— **静默退化成简单均值**，
而数值看上去完全正常。

修法是给递归留出历史（`4 × ema_period` 额外根，种子的残余影响
`(1−2/(p+1))^(4p) ≈ 3e-4`）。修完 `stretch` 的横截面结果从 **+2.1% 变成 +22.6%**
（11 倍）—— 这个 bug 严重压制了信号。

**它是被测试抓到的**，而且抓到的方式值得记：第一版测试只断言排序
（`calm < wild`），而两个符号的均值有微小差异，所以**排序在 bug 存在时仍然成立**，
差距只从 50 倍缩到接近零。把断言改成**幅度**（`|calm| > 5·|wild|`）才捕获。
另外，`realized_vol` 自己的缺口保护**掩盖**了 EMA 层的缺口保护 —— 只有当波动率
窗口比 EMA 窗口**短**时才可观测，所以补了一条 `stretch_signal(40, 10)` 的测试。

变异验证 8/8 全部捕获（含「缩回恰好 period 根」这一条原 bug）。

#### 我的预测

P1（基线最强）、P2（RSI/stretch 更弱）**都对**；P3（两者不是同一效应）**也被 2×2
证实**。这是本会话第一次三项预测全部正确 —— 前两次（§二.8、§二.15）都偏悲观。

#### 结论

> **⚠ 2026-09-22 更正（见 §二.21）：本节关于横截面的正面结论在跨窗口意义上不成立。**
>
> 在 6 个不重叠窗口上重做，横截面的 **t > 2 只出现在 1/6 个窗口**，净收益 5/6 为正
> 但幅度从 −24.3% 到 +40.5%。本节所有的 +43.0% / t=1.28 来自**单个 170 天窗口**。
>
> **2×2 的那个结论仍然成立**（时序版在两半都有效、横截面只在非流动有效）——
> 它测的是「效应在哪里」，跨窗口测的是「效应是否稳定」，两者不矛盾。
> 不成立的是**幅度与显著性**。

- 横截面 RSI **在仓库已测量的效应范围之内**，没有新增独立信息，且弱于直接用量价
  反转。所以它**不替代**基线。**（幅度不跨窗口，见上面的更正）**
- 时序版**是独立的东西**，不是前者的近似 —— 这是本次最主要的结论。
  **并且它经跨窗口检验复现了（6/6 窗口，§二.21）。**
- ~~下一步的自然问题：两者能否叠加~~ **已测（§二.20、§二.21）：横截面侧不稳，
  组合因此不成立。**

---

### 18. 非流动一侧是不是微观结构假象：不是，但它成本敏感

§二.17 结尾指出：时序与横截面**都在非流动标的更强**，而陈旧报价在 bid/ask 之间
来回跳会被读成均值回归。**若这是假象，前两轮的正面结论同时作废** —— 所以这是
当时最大的单点风险。

#### 可证伪的三个预测

| | 预测 | 依据 |
|---|---|---|
| **P1** | 效应只存在于 lag-1，更长持有期上消失 | 价差跳动只影响相邻两根；真实过度反应会持续 |
| **P2** | 效应大小**正比于**估计的买卖价差 | 若为捕捉价差，宽价差标的应更强 |
| **P3** | 效应**不超过**估计的往返成本 | 否则「赚的」就是要付的价差 |

#### P1 被否定：持有期扫描的形状与跳动完全相反

非流动 86 标的，`negate(past_return(24))`：

| 持有期 | 均值/期 | t | **换成每 bar** |
|---|---|---|---|
| **1** | **−0.014%** | **−0.90** | −0.014% |
| 2 | −0.006% | −0.24 | −0.003% |
| 4 | +0.062% | 1.81 | +0.016% |
| 8 | +0.190% | 3.14 | +0.024% |
| 24 | +0.204% | 1.28 | +0.009% |
| 48 | +0.722% | 2.71 | +0.015% |
| 120 | +1.853% | 2.12 | +0.015% |

**在 hold=1 —— 跳动唯一存在的地方 —— 效应是零。** 它随持有期增长，每 bar 收益在
hold≥4 之后稳定在 ~0.015%。**若是微观结构，hold=1 应最强并迅速衰减。**

新增的 `microstructure.bounce_share` 给出一个直接数字：

| 分组 | **lag-1 占负相关的比例** | 方差比(4) |
|---|---|---|
| 流动 | **0.277** | 0.9473 |
| 非流动 | **0.307** | 0.9268 |

**只有约 28–31% 的负自相关落在 lag-1** —— 绝大部分不在，所以不是跳动。

#### P2 被否定：效应与价差不相关，且符号在两组间翻转

| 分组 | corr(价差, 效应) | 窄价差一半 | 宽价差一半 |
|---|---|---|---|
| 非流动 | **−0.158** | +16.7% | **+11.5%**（更低） |
| 流动 | +0.182 | +6.2% | +18.9% |

若为捕捉价差，两组都该显著为正。实测**弱相关且符号不一致**。

#### P3 是真问题：效应只在长持有期、且显著性勉强

把 `fee_bps` 设成真实价差水平（非流动，累计收益 / t 值）：

| 持有期 | 3.5bp | 10bp | 20bp | **38bp（Roll）** | 换手 |
|---|---|---|---|---|---|
| 8 | +196.5% / 3.14 | +105.8% / 2.15 | **+17.3% / 0.62** | **−57.4% / −2.14** | 46% |
| 24 | +43.0% / 1.28 | +16.8% / 0.65 | −14.5% / −0.32 | −51.2% / −2.05 | 77% |
| **48** | +98.4% / 2.71 | +79.4% / 2.33 | +53.6% / 1.74 | **+16.1% / 0.69** | 78% |
| **120** | +93.9% / 2.12 | +86.4% / 2.00 | +75.3% / 1.82 | **+57.0% / 1.49** | 79% |

**高换手的短期限版本最脆弱**（hold=8 在 20bp 下就崩到 t=0.62），而**长持有期在
38bp 下仍为正，但 t 只有 0.69/1.49 —— 不显著。**

#### 一个必须说清的估计问题

在归档上 **lag-24 的负自相关和 lag-1 一样大**（流动 −0.0287 vs −0.0258）。
Roll 估计**假定负自协方差全部来自价差跳动**，所以它把真实回归也算了进去 ——
**它是上界，不是测量**。对这批成交额百万级的标的，38bp 的单边价差高得可疑
（真实应是个位数 bp），所以上表里 38bp 那一列应读作**悲观情景**而非事实。

把它当事实会得出「效应负收益」的错误结论；当上界用才是对的。上面已经按这个
方式用（给出 3.5–38bp 的区间而不是一个数）。

#### 结论

> **⚠ 2026-09-22 更正（见 §二.19）：下面这条「接近成本量级」的判断是错的。**
>
> 它建立在本节表格里那个 **38bp** 的成本列上，而那正是本节自己指出的
> **Roll 上界**。实测实盘盘口，票池的中位顶档价差是 **3.04 bp** —— Roll 高估了
> **约 10 倍**。按真实成本（$1k 规模 7.2bp/边）重跑，效应显著性为
> **t = 2.1–2.6，存活**。
>
> 把上界当测量用，方向是悲观，而悲观和乐观一样会误导。这个错误我犯在
> 警告过它的同一章里 —— 保留原文，让这个模式可见。

- **不是微观结构假象**：P1、P2 都被否定，而且否定得干净（hold=1 是零、lag-1
  占比只有 28–31%）。这是本次最主要的结论。
- ~~**但它是成本敏感的**：…显著性只剩 t≈0.7–1.5。§二.17 的正收益是成本乐观口径。~~
  **已更正**：那是 Roll 上界列。真实成本下效应存活，但受**容量**约束（§二.19）。
- ~~效应真实、但幅度接近成本量级。~~
  **已更正**：效应真实，幅度**大于** $1k 规模的成本，但小于 $10k 规模的成本。

#### 落地

`backtest/microstructure.py`：自相关/自协方差、`roll_spread`、`variance_ratio`、
`reversal_profile`、`bounce_share`。24 条测试，7/7 变异捕获。模块**精确复现**
手算的归档数字（流动 lag1=−0.0258 / lag24=−0.0287；非流动 −0.0318 / −0.0249）。

模块文档明确写了它**不能**做什么：**没有任何统计量能判断效应是否可交易** ——
一个完全真实的效应也可能小于你必须跨过的价差。那是成本问题，只能靠「把费率
设成价差重跑」回答，不是这个模块里的任何数字。

#### 我的测试夹具错了四次

| 失败 | 原因 |
|---|---|
| Roll 测试差 100 倍 | 我断言**价格单位**的价差，估计器返回**相对**价差。模块是对的 |
| 方差比 >2 未达成 | 常数漂移 + 噪声的方差比是 ~1；要 >1 需**正自相关** |
| 缺口测试 −0.669 而非 <−0.9 | **真实性质**：丢弃缺口把同号值接在一起，**把自相关往零偏**（方向值得记） |
| 「宽跳动」估计更小 | 夹具命名反了：`±0.01` 在相对量上**小于** `±0.02` |
| 完美交替 share=0.5 | **完美交替不是真实跳动**（它所有奇数 lag 都为负）；真实跳动只有 lag-1 |

两次变异（`bounce_share` 分母、缺口桥接）第一版也漏掉：前者是我的变异本身无效，
后者是我的断言「仍然很负」被「桥接为 0」也满足 —— 改成**逐值相等**（丢弃后的
结果必须等于「那些 bar 从未存在」）才捕获。

---

### 19. 真实成本口径：Roll 高估了 10 倍，效应在小规模下存活

§二.18 结尾留下三处独立测量指向同一个瓶颈，结论是「效应真实、**幅度接近成本量级**」。
**这个结论里有一处错误，必须先更正。**

#### 更正：我把 Roll 的上界当成了成本

§二.18 的成本敏感性表里有一列 **38bp**，我在结论里用它说「显著性只剩 t≈0.7–1.5」。
但同一节的正文里我自己写着：**Roll 是高估的上界**，因为归档上 lag-24 的负自相关
和 lag-1 一样大，而 Roll 假定负自协方差**全部**来自价差。

**把上界当测量用，方向是悲观** —— 而悲观和乐观一样会误导，当它被用来否证一个
发现时。这正是模块文档里警告过的错误，我却在同一章里犯了。

实测一下就知道差多少（`probe_spreads.py`，实盘 L2 盘口）：

| | 值 |
|---|---|
| Roll 估计（票池） | **29.9 / 33.8 bp** |
| **真实顶档价差（票池中位）** | **3.04 bp** |

**高估约 10 倍。**

#### 真实成本：随规模变化，而且随深度

取实盘 L2 盘口，按不同下单规模走单计算往返成本（`microstructure.round_trip_cost_bps`）：

| 分组 | n | 顶档 | **$1k 单边** | **$10k 单边** | 50bp 内深度 | $10k 可成交 |
|---|---|---|---|---|---|---|
| 流动一半 | 82 | 1.75 bp | 1.85 bp | 5.08 bp | $35k | 70/82 |
| 非流动一半 | 82 | 3.86 bp | 4.96 bp | **19.60 bp** | **$13k** | 63/82 |
| **票池合计（中位）** | — | **3.04 bp** | **3.67 bp** | **11.40 bp** | **$18k** | — |

加上 taker 费 3.5bp：

- **$1k 规模：7.2 bp/边**（回测假设 3.5bp，乐观 2 倍）
- **$10k 规模：14.9 bp/边**（乐观 4.3 倍）

**而且 50bp 内的中位深度只有 $18k** —— 一个 $10k 的订单要吃掉**一半以上**的可见
流动性。**容量是比成本更硬的约束。**

#### 用校准成本重跑

| 成本假设 | hold=8 | hold=24 | hold=48 | hold=120 |
|---|---|---|---|---|
| 回测原假设 3.5bp | +196.5% (t3.1) | +43.0% (t1.3) | +98.4% (t2.7) | +93.9% (t2.1) |
| 盘口成交 ~5bp | +172.5% (t2.9) | +36.4% (t1.1) | +93.8% (t2.6) | +92.2% (t2.1) |
| **$1k 实测 7.2bp** | **+140.9% (t2.6)** | +27.4% (t0.9) | **+87.3% (t2.5)** | **+89.6% (t2.1)** |
| **$10k 实测 14.9bp** | +56.3% (t1.4) | +0.3% (t0.2) | **+66.3% (t2.0)** | +80.9% (t1.9) |

流动一半在每种成本下都是偶然（t ≤ 1.2）—— 这反过来支持「效应确实在非流动一侧」，
因为成本假设的变化没有把它推过门槛。

**结论（修正后）**：效应**在 $1k 规模下存活**（t = 2.1–2.6），**在 $10k 规模下
勉强**（t = 1.4–2.0），**再往上被容量挡住**。不是 §二.18 说的「接近成本量级」。

#### 必须带着的两个限定

1. **盘口是今天的，回测窗口是 2–9 月**，差几个月。所以这批数字校准的是**量级**
   （「几个 bp，不是几十个 bp」），不是「某个历史 bar 上的成本」。要做严格版本需要
   历史盘口数据，归档里没有。
2. **执行成本不等于全部成本**：还有市场冲击、逆向选择、以及**资金费**（多空两侧
   都要付）。本节的数字只覆盖价差与吃穿档位。

#### 落地

- `microstructure.book` 系列：`top_of_book_spread_bps`、`round_trip_cost_bps`（走单）、
  `depth_within_bps`。纯函数，34 条测试，5/5 变异捕获。
- `probe_spreads.py`：实盘采样，输出上表。**它自己会标记有偏的行** —— `$100k` 那一行
  在流动组只有 3/25 能成交，中位数是在极小且有偏的子集上算的，脚本打上
  `<- biased: median is over the deep names only`。这个坑我自己先踩过一次
  （$100k 显示 0.55bp，比 $10k 还便宜）。

**这一步没有找到新信号，它做的是另一件事：把前四章的地基测清楚。** 结果是
**§二.18 的悲观结论被推翻，§二.15 和 §二.17 的正面结论站得住了** —— 但要加上
「$1k 规模、容量 $18k 量级」这两个限定。

---

#### 282 天历史盘口的复核（2026-09-24，第十四轮同步）

第十四轮把 `orderbook` 归档补齐了（`by_dex/hyperliquid/orderbook/1m/perps`，282 天 ×
BTC/ETH/SOL，每分钟一个快照、每档 20 层）。它第一次让成本测量从「一次实盘探针」
变成「282 天的时间序列」：

| 币 | 快照数 | 顶档价差中位 | p25 | p75 |
|---|---|---|---|---|
| BTC | 405,569 | **0.140 bp** | 0.13 | 0.15 |
| ETH | 405,569 | **0.473 bp** | 0.41 | 0.53 |
| SOL | 405,569 | **0.131 bp** | 0.12 | 0.73 |

BTC 的 0.140 bp 几乎正好是**一个 tick**（$1 / $86k = 0.116 bp）。

中位快照的卖一侧深度（名义）：

| 币 | 1 bp 内 | 5 bp 内 | 10 bp 内 |
|---|---|---|---|
| BTC | **$916,336** | $4,022,482 | $4,023,035 |
| ETH | **$400,074** | $4,640,627 | $9,568,255 |
| SOL | **$76,289** | $555,280 | $611,903 |

**结论：我们的 7.2 bp/边 几乎全是手续费，不是市场结构。** 本 bot 的名义额约 $100，
而 1 bp 内就有 $7.6万–$92万 的深度 —— 订单比深度小约四个数量级，走盘口的成本可忽略。
按 taker 3.5 bp 双边计正好 7 bp，与实测 7.2 bp 吻合。

这与 §2.25 的结论一致（**能动的是费率，不是换手**），但现在它有 282 天的依据，
而不是一次探针。

两点如实标注：归档只存 20 档，所以 BTC 的 10 bp 列等于 5 bp 列、SOL 的两列也很接近 ——
**10 bp 档是截断值**，不是真实深度；以及上表是**卖一侧**（买方吃单的方向）。

### 20. 时序择时 + 横截面选股：独立，但组合优势不复现

§二.17 的结论是两者**不是同一个效应**，所以「叠加」是一个有理由的假设而非盲试。
这一步把它测了。**答案分两半：独立性成立，叠加优势不成立。**

#### 测法：同一组窗口上的收益序列

不能把两个回测的数字相加 —— 那只是把两个不同风险的口径混在一起。正确做法是把
两者放在**同一组 24 根 bar 的非重叠窗口**上（各 202 个），得到两条对齐的收益序列，
再算相关性、风险配权与分段。

- 横截面：`negate(past_return(24))` 多空五分位，非流动 86 标的
- 时序：RSI14 均值回归，同一批标的的等权组合
- 成本：§二.19 实测的 **7.2bp/边**（$1k 规模）

#### 预测

| | 预测 | 结果 |
|---|---|---|
| **P1** | 两条序列弱相关（\|ρ\| < 0.4） | **对**：ρ = −0.016 |
| **P2** | 组合 Sharpe 高于任一单独 | **错**：等资本组合 1.21 < 时序单独 1.28 |
| **P3** | 提升有限 | **错**：是**下降** |

#### P2 为什么错：等资本不是等风险

两条序列的波动率差 **3.94 倍**（2.271% vs 0.577% / 窗）—— 横截面是双腿满仓的多空
篮子，时序版只有 ~16% 敞口。**所以「50/50 资本」实际是「80/20 风险」**，弱策略
按风险压倒了强策略。这不是「不能叠加」，是**权重方式错了**：

| 权重方案 | w(XS) | w(TS) | 累计 | **Sharpe** |
|---|---|---|---|---|
| 等资本 50/50 | 0.50 | 0.50 | +20.5% | 1.21 |
| **等风险（逆波动）** | **0.20** | 0.80 | +14.9% | **1.56** |
| 单独 横截面 | 1.00 | 0 | +27.4% | 0.92 |
| 单独 时序 | 0 | 1.00 | +10.6% | 1.28 |
| 扫描最优 | 0.15 | 0.85 | — | **1.58** |

**按风险配权，Sharpe 从 1.28 提到 1.56（+22%）。**

#### 但分段否定了它 —— 和 §二.16 止损那轮同一种模式

| 区段 | n | corr | XS Sharpe | TS Sharpe | 等风险组合 | 最优 w(XS) |
|---|---|---|---|---|---|---|
| 全样本 | 202 | −0.016 | 0.92 | 1.28 | **1.56** | 0.15 |
| **前半** | 101 | −0.022 | **0.00** | **0.08** | **0.06** | 0.00 |
| **后半** | 101 | −0.021 | 1.41 | **3.04** | 2.81 | 0.05 |
| 奇偶交替A | 101 | −0.020 | 0.14 | 1.07 | 0.86 | 0.05 |
| 奇偶交替B | 101 | −0.007 | 1.35 | 0.73 | 1.47 | 0.35 |

**全样本的 1.56 是 0.06 与 2.81 的平均 —— 它不描述任何一段。**
前半两个策略的 Sharpe 都是零。

**只有相关性是稳定的**（−0.007 ~ −0.022，五段全部一致）。而要注意：n=202 时相关系数
的标准误约 0.07，所以**「独立」的准确说法是「与独立不可区分」**（±0.14 以内测不出），
不是「已证明独立」。

#### 我差点把一个事后描述写成发现

看到全样本结果后，我按「该窗市场方向」分档，得到一个很干净的模式：两个策略都在
**下跌窗口赚钱、上涨窗口亏钱**（跌1/3：XS Sharpe 1.66、TS 1.33；涨1/3：−0.12、−0.56）。
这看起来像是「均值回归在下跌中收过度反应」的机制确认。

**但它在前半里是反的**：

| | 跌 1/3 | 涨 1/3 |
|---|---|---|
| **前半** XS | **−0.031%** | −0.237% |
| **后半** XS | **+0.444%** | +0.065% |

前半的「跌1/3」对两个策略都是负的。**所以那个 regime 模式完全是后半驱动的，
是事后描述，不是机制。** 检验它的那个动作（在半段内重做分档）才是这一步的价值。

#### 结论

- **两者独立**（这一点是稳的，五段一致）—— 所以叠加在**构造上**是合理的方向。
- **但叠加优势在本样本上不成立**：后半贡献了全部信号，前半没有。全样本的
  Sharpe 提升是两段的平均，不是可复现的性质。
- **同样的失败模式第二次出现**（第一次是 §二.16 的止损）。所以下一步不该是再调权重
  —— 调权重只会在同一份数据上找另一个全样本最优。

#### 落地

`backtest/combine.py`：`correlation` / `sharpe` / `risk_parity_weights` / `blend` /
`weight_sweep` / `halves` / **`stability`**。22 条测试，8/8 变异捕获。

`stability()` 把**全样本与前/后半放在同一次返回里**，这样调用者不可能只拿到
全样本数字就写进结论 —— 本仓库两次失败都发生在这一步，而两次被引用的都是全样本。

#### 顺带发现：我的变异验证工具本身有个漏洞

这一轮变异第一遍报「复原: 1」，我以为测试有问题。实际是 **Python 用了陈旧的
`__pycache__`**：变异把 `0.0` 改成 `1.0`，**长度相同**，而写入常落在同一秒内，
于是 mtime + size 双双相符，缓存不被失效 —— **变异被静默吞掉，看起来像「测试没抓到」。**

修法是**每条变异都改变文件长度**（或在 `run()` 前 `os.utime`）。已回查前面几轮报
「捕获 0」的两处（§二.15 M7、§二.18 M6），那两处**确实是变异本身无效**（一个加了
无用变量、一个 `.replace` 后变回原字符串），不是缓存假象。但这个陷阱值得记下来：
**变异验证失败时，「测试弱」和「变异没生效」是两种不同的原因，必须分开排除。**

---

### 21. 跨窗口重复：一个效应真的复现了，而且不是我们一直引用的那个

§二.20 的结论是「组合优势不复现」。而在同一份数据上继续调权重只会找到另一个
全样本最优 —— 所以这一步做的是**扩样本**：把归档里所有 415 天切成 **6 个不重叠的
69 天窗口**，每个窗口**独立选池**（归档里每日标的数从 252 涨到 890，固定票池会有偏），
然后重复核心测量。

#### 结果

| 窗口 | 标的 | XS 净% | XS t | **TS 超 p75** | corr | 组合 Sharpe |
|---|---|---|---|---|---|---|
| 25-07-31~10-07 | 82 | +6.3% | 0.42 | **35/82 (43%)** | −0.034 | 0.56 |
| 25-10-08~12-15 | 90 | −24.3% | −0.70 | **34/90 (38%)** | −0.087 | −0.75 |
| 25-12-16~02-22 | 91 | +40.5% | **3.13** | **44/91 (48%)** | +0.006 | 2.06 |
| 26-02-23~05-02 | 92 | +20.8% | 1.19 | **37/92 (40%)** | +0.071 | 1.67 |
| 26-05-03~07-10 | 87 | +2.8% | 0.18 | **51/87 (59%)** | +0.019 | 0.28 |
| 26-07-11~09-17 | 87 | +9.6% | 0.79 | **41/87 (47%)** | −0.119 | 1.15 |
| **合计** | | 中位 +7.9% | 中位 0.60 | **242/529 (46%)** | 中位 −0.01 | 中位 0.85 |

**窗口级符号检验**（6 个窗口是独立单位，这是最保守的判据）：

| 判据 | 结果 | 符号检验 P |
|---|---|---|
| **TS 超 p75 > 25%** | **6/6** | **0.031** |
| XS 净收益 > 0 | 5/6 | 0.219 |
| XS t > 2 | **1/6** | — |
| 组合 Sharpe > 1 | 3/6 | ~1.0（无证据） |

#### 结论一：TS 的择时优势跨窗口复现，这是本仓库第一个

> **⚠ 2026-09-22 续记（§二.24）：这个指标是成本盲的，不能用来判断盈亏。**
>
> 实测把它在 0 / 4 / 7.2 / 20 / 50 bp 五个成本点上重算，结果**每一位都相同**
> （368/844 = 43.6%）—— 因为 `random_control` 用同一费率，两边扣同样多，
> 相对排名不变。
>
> 所以本节的结论**只在「择时技巧存在」这一层成立**，而**不能**读成
> 「策略可行」。§二.24 测出这个技巧的价值约 **2.5–4 bp/边**，而实测成本
> **7.2 bp/边** —— **在真实成本下不盈利**。
>
> 本节没有说错「技巧复现」，错的是**我拿它代替了盈亏判断**。留着原文，
> 因为这个替换很自然、也很难自己发现。

**「超 p75」是 6/6 个窗口都高于 25%**（范围 38%–59%，不是勉强过线），
合计 242/529 = 46% 对期望 25%。**这是本会话唯一一个在独立窗口上复现的东西。**

#### 结论二：它同时解开了 §二.20 的谜

§二.20 里 TS 在 170 天窗口的**前半 Sharpe 是 0.08，后半是 3.04**，看起来效应不存在。
但那是**原始收益**的 Sharpe —— 而表格里 TS 的**净收益**在跨窗口时同样剧烈波动
（+122% / **−993%** / −273% / +395% / +108% / +147%）。

**原始收益里含市场 beta，它在不同窗口差别巨大；而「超 p75」用的是同敞口随机对照，
把 beta 消掉了。** 所以：

| 指标 | 跨窗口表现 |
|---|---|
| TS 原始收益 | **剧烈波动**（−993% ~ +395%） |
| TS 超 p75（配对敞口） | **6/6 稳定**（38%–59%） |

**§二.20 得出的「效应不存在」是用了错误的指标。** 正确的说法是：**TS 的择时优势
稳定，它的原始收益不稳定** —— 后者本来就该不稳定，因为它带着市场方向。

这也回过头修正 §二.15：那一节的 112/216 用的是「超 p75」，与这里一致，所以它
**是对的**；而我在 §二.20 用原始收益的 Sharpe 去否定它，是换了尺子不知道。

#### 结论三：横截面侧没有复现

XS 净收益 5/6 为正，但 **t > 2 只在 1/6**（唯一显著的是 25-12-16~02-22 那个
+40.5%、t=3.13 的窗口）。**§二.17 里 XS 的正面结论在跨窗口意义上不成立。**

而 §二.20 的组合 Sharpe（1.56）建立在 XS 之上，所以它**同样不成立** ——
组合 Sharpe > 1 只有 3/6。现在原因清楚了：**不是权重问题，是 XS 一侧本身不稳。**

#### 四分五裂的 corr

corr 在 6 个窗口里是 −0.034 / −0.087 / +0.006 / +0.071 / +0.019 / −0.119 ——
**幅度都小（|ρ| ≤ 0.12）但符号翻转。** 所以「独立」这个说法成立（一致地接近零），
但**不能声称一个稳定的负相关**。

#### 我这一轮的三条预测

| | 预测 | 结果 |
|---|---|---|
| **P1** | 效应只在部分窗口出现 | **对**：XS t>2 只有 1/6 |
| **P2** | 窗口间离散度大到让均值不可信 | **对**：XS −24%~+41%，TS −993%~+395% |
| **P3** | 相关性比收益稳定 | **部分对**：幅度稳定地小，但符号翻转 |

#### 落地

`combine.py` 增加：`sign_test`（窗口级符号检验）、`equal_windows`（等长不重叠切分）、
`replicates`（计数 + p 值打包）。35 条测试，3/3 变异捕获。

**`sign_test` 实现里我自己写错了一处**，被测试抓到：正确的双侧二项是
`2 × min(下尾, 上尾)`，我只加倍了上尾并加 cap —— 于是 `1/6` 给出 P = 1.0
而不是 0.219，破坏了与 `5/6` 的对称性。一条对称性测试抓到了它。
而修完之后，**我那条测试自己又错了一处**：它把 `(0, 5)` 列进了「无证据」的例子，
而 0/5 是 1/32 的概率，P = 0.0625。

#### 这一步改变了什么

- **§二.15 的 TS 结论被跨窗口证实**（6/6 窗口），并且找到了它此前看起来不稳的原因
  —— **用了含 beta 的原始收益做判据**。
- **§二.17 的 XS 结论在跨窗口意义上不成立**（t>2 仅 1/6）。
- **§二.20 的「叠加优势」因此也不成立**，且原因不是权重。
- **§二.19 的成本测量仍然成立**（它是独立于收益口径的直接观测）。

**所以下一步有个明确的、也是唯一剩下的方向：以「超 p75」为准绳，把 TS 规则本身
做扎实**（出场扫描、跨标的验证），而不是再碰横截面组合。

---

### 22. 出场扫描：单窗口找到一个「90%」的赢家，跨窗口它是最差的那个

§二.15 遗留的问题：出场规则从没扫过。§二.10 已证明出场决定成本结构，而当时
扫描用的是假设的 3.5bp，现在成本是实测的 **7.2bp/边**（§二.19），所以该重扫。

**判据用「超 p75」而不是原始收益**（§二.21 的教训）：原始收益含市场 beta，
跨窗口它是 −993% ~ +395%，拿它给出场排名等于按「在场时间多长」排序 ——
而那是出场的定义，不是它的质量。

#### 单窗口（170 天）：exit=35 看起来是个大赢家

| exit | 交易 | 均持仓 | 敞口 | 净均值 | **超 p75** |
|---|---|---|---|---|---|
| **35** | 4429 | 5.6 | 5.9% | +20.45% | **77/86 (90%)** |
| 40 | 3624 | 10.2 | 8.8% | +14.99% | 62/86 (72%) |
| 45 | 3219 | 15.9 | 12.2% | +13.29% | 50/86 (58%) |
| **50（默认）** | 2901 | 23.9 | 16.5% | +11.23% | 48/86 (56%) |
| 60 | 2337 | 49.6 | 27.4% | +14.14% | 49/86 (57%) |
| 70 | 1551 | 140.4 | 50.7% | +12.10% | 43/86 (50%) |
| 80 | 639 | 571.4 | 80.6% | +6.51% | 38/86 (44%) |

`exit=35`（RSI 一到 35 就出，均持仓 5.6 根）比默认的 50 高出 34pp。
**而 exit=80 掉到 44%** —— 持仓 571 根、敞口 81%，它已经变成事实上的多头，
这一点符合预期。

#### 我先做了两个证伪检查，都通过了

90% 是个红旗，所以先排除两种假象：

**（1）随机对照只跑 20 次太粗？** 提到 100 次和 200 次：

| exit | 20 次 | 100 次 | 200 次 |
|---|---|---|---|
| 35 | 77/86 (90%) | 77/86 (90%) | 78/86 (91%) |
| 50 | 48/86 (56%) | 46/86 (53%) | 46/86 (53%) |
| 80 | 38/86 (44%) | 37/86 (43%) | 37/86 (43%) |

**不变。** 不是对照样本量的问题。

**（2）只是成本假象？** 看毛收益：

| exit | 净均值 | **毛均值** | 随机对照均值 | 实盘−对照 |
|---|---|---|---|---|
| 35 | +20.45% | **+29.75%** | −6.57% | **+27.02pp** |
| 50 | +11.23% | +16.81% | −5.21% | +16.44pp |
| 80 | +6.51% | +7.71% | −5.06% | +11.57pp |

**形状在毛收益上也在**，且随机对照一致为负。不是成本假象。

#### 跨窗口：它是最差的那个

6 个不重叠窗口、每窗独立选池：

| 窗口 | exit=35 | exit=45 | **exit=50** | exit=60 | exit=70 |
|---|---|---|---|---|---|
| 25-07-31~10-07 | 62% | 60% | 43% | 40% | 37% |
| 25-10-08~12-15 | **11%** | 20% | 38% | 50% | 51% |
| 25-12-16~02-22 | **14%** | 43% | 48% | 49% | 29% |
| 26-02-23~05-02 | 53% | 40% | 40% | 54% | 68% |
| 26-05-03~07-10 | 85% | 62% | 59% | 48% | 38% |
| 26-07-11~09-17 | 57% | 43% | 47% | 49% | 45% |

| exit | 中位 | **范围** | >25% 的窗口 | **符号检验 P** |
|---|---|---|---|---|
| **35（单窗口最优）** | 55.4% | **[11.1%, 85.1%]** | **4/6** | **0.6875** |
| 45 | 42.7% | [20.0%, 62.1%] | 5/6 | 0.2188 |
| **50（默认）** | 44.9% | [37.8%, 58.6%] | **6/6** | **0.0312** |
| 60 | 49.4% | [40.2%, 54.3%] | **6/6** | **0.0312** |
| 70 | 41.4% | [28.6%, 68.5%] | **6/6** | **0.0312** |

**单窗口那个 90% 的赢家，跨窗口是 4/6、P=0.69；而默认的 50 是 6/6、P=0.031。**
它的中位数（55.4%）看着最高，但那只说明它有时很好 —— 范围 [11%, 85%] 才是重点。

#### 结论：不采纳任何改动，保留默认

**出场扫描没有找到改进，而唯一「看起来是改进」的那个值是最不稳的。**
稳健区间是 **50–70**，也就是「等 RSI 回到中性或更高再出」—— 这正是入场规则的
常规镜像读法。所以：

- **保留 `exit_level=50`。**
- 若将来要动它，**必须跨窗口至少一样稳**，而不是在单个窗口上赢。

#### 这一步的元结论

这是**同一种失败模式第三次出现**（§二.16 止损、§二.20 叠加、§二.22 出场）：
**单窗口最优，跨窗口不复现。** 三次里唯一的区别是这次我**先**做了跨窗口检查、
再下结论，而前两次是先下了结论。所以纪律本身比这次的数字更值钱：

> **任何「这个配置更好」的判断，都必须先跨窗口，再引用。**

#### 落地

`scan_exits.py`：扫描 + 跨窗口验证，两个视图**打印在一起**（单窗口在上、跨窗口在下），
所以那个误导性的 90% 不会被总结掉。`--days 170` 可复现单窗口视图。

---

### 23. 流动性五档：效应存在于四档，而第五档恰好是唯一好交易的

§二.17 只把票池切成两半，结论是「两半都有、非流动更强」。而 §二.19 发现效应的
可交易性受**容量**限制。这两件事都靠「两半」这一个分辨率，所以这一步细分到
**流动性五档**，并把每档的真实盘口容量与之并列 —— 因为「效应在哪里」和
「能不能交易」是同一个问题的两端。

#### 效应按档（6 个不重叠窗口 × 5 档，每窗独立分档）

| 窗口 | Q1 | Q2 | Q3 | Q4 | Q5 |
|---|---|---|---|---|---|
| 25-07-31~10-07 | 36% | 55% | 33% | 45% | 27% |
| 25-10-08~12-15 | 36% | 39% | 31% | 36% | 39% |
| 25-12-16~02-22 | 53% | 47% | 44% | 31% | 24% |
| 26-02-23~05-02 | 51% | 32% | 30% | 32% | 24% |
| 26-05-03~07-10 | 47% | 76% | 59% | 56% | 34% |
| 26-07-11~09-17 | 54% | 51% | 34% | 40% | 31% |

| 档 | 中位成交额/h | 中位比例 | 范围 | **>25% 窗口** | **符号检验 P** |
|---|---|---|---|---|---|
| **Q1** | $3,075 | **49.2%** | [36.1, 54.3] | **6/6** | **0.031** |
| **Q2** | $4,626 | **49.3%** | [32.4, 76.5] | **6/6** | **0.031** |
| **Q3** | $8,234 | 33.8% | [29.7, 58.8] | **6/6** | **0.031** |
| **Q4** | $32,460 | 38.1% | [30.6, 55.9] | **6/6** | **0.031** |
| **Q5** | $214,423 | 29.4% | [23.7, 38.9] | **4/6** | **0.688** |

**效应在前四档全部 6/6（P=0.031），在最流动的 Q5 不存在（4/6，P=0.69）。**

三条预测里 **P1 对**（最不流动两档最强：49.2%、49.3%）、**P2 错**（我以为每档都有）、
**P3 错**（梯度不单调：Q3 的 33.8% 低于 Q4 的 38.1%）。所以准确的说法不是
「效应随流动性单调变化」，而是**「有一个上限：效应止于最流动的一档」**。

#### 把它和真实容量并列 —— 这是本节的重点

用实盘 L2 盘口量同一分档下的容量（`probe_spreads.py` 的方法）：

| 档 | 成交额/h | **50bp 内深度** | 顶档价差 | $1k 单边执行 | **效应** |
|---|---|---|---|---|---|
| Q1 | $3,075 | **$8,884** | 4.65bp | 6.84bp | **49.2% 6/6** |
| Q2 | $4,626 | **$13,558** | 3.60bp | 5.25bp | **49.3% 6/6** |
| Q3 | $8,234 | $12,249 | 3.78bp | 4.96bp | **33.8% 6/6** |
| Q4 | $32,460 | **$27,231** | 2.48bp | 2.18bp | **38.1% 6/6** |
| **Q5** | $214,423 | **$79,889** | **1.08bp** | **1.27bp** | 29.4% **4/6** |

**效应存在的四档，50bp 深度只有 $9k–27k；而深度最大（$80k）、成本最低（1.08bp）
的 Q5 恰好没有效应。**

这就是整条研究线中心问题的**最尖锐形式**：

> **效应的存在性与可收割性反向。** 效应越大、越稳的档位，书本越薄、价差越宽；
> 而书本最厚、最便宜的那一档，效应消失。

§二.19 只给出了「$1k 规模下效应存活、50bp 深度 $18k」，§二.23 把它变成了
**一张按档位的对照表** —— 而表里没有一行是「效应强且便宜」的。

#### 结论

- **效应不是普遍的**：它有一个**流动性上限**，止于最流动的一档（Q5）。
  这与「纯微观结构假象」不同（那样应该是越不流动越强、且 Q1 最强），
  但也是为什么「往上走、用更深的书」这条扩容量路线**走不通**。
- **容量约束是真的**：效应所在的四档深度是 $9k–27k，不是「小一点」而是
  **小两个数量级**。在 §二.19 的 $10k 规模上效应已经勉强（t=1.4–2.0），
  而 $10k 已经吃掉 Q2/Q3 一半以上的可见深度。
- **所以「扩容量」这个方向被否掉了** —— 剩下的只有「接受低容量」或「换信号」。

#### 落地

`universe.liquidity_quintiles`（等长分档，最不流动在前）。50 条 `test_universe`
测试，5/5 变异捕获。

变异验证里这一轮又踩了一次坑：`dv` 全置零的变异**没有被捕获**，因为它退化成
按字母排序，而我的夹具恰好是 A 在前 —— **是变异本身无效，不是测试弱**。
换成「`liquidity_medians` 改用均值」之后立即捕获。这与 §二.20 记的那条同源：
**变异没被抓到时，「测试弱」和「变异没生效」必须分开排除。**

---

### 24. 成本敏感性：择时技巧值 2.5bp/边，而成本是 7.2bp/边

§二.19 只给了 $1k/$10k 两个成本点，§二.23 给了分档容量，但都没有把判据画成成本的
函数。这一步补上，而它顺带**推翻了我自己上一轮的判据选择**。

#### 先发现的问题：「超 p75」在构造上就是成本盲的

实测（Q1–Q4，6 窗合并，随机对照用同一费率）：

| 成本 | **超 p75** | 比例 | 正收益比例 | 中位净收益 |
|---|---|---|---|---|
| 0.0bp | 368/844 | **43.6%** | 54.4% | +1.37% |
| 4.0bp | 368/844 | **43.6%** | 51.2% | +0.32% |
| **7.2bp** | 368/844 | **43.6%** | **49.1%** | **−0.39%** |
| 20.0bp | 368/844 | **43.6%** | 39.8% | −3.61% |
| 50.0bp | 368/844 | **43.6%** | 17.7% | −10.29% |

**在每一个成本点上都是 368/844 = 43.6%，一位不差。**

原因是机械的：`random_control` 用**同一个费率**，所以两边每笔都被扣掉相同的期望
金额，**相对排名完全不变**。所以「超 p75」衡量的是**择时技巧**，
**它在构造上无法回答「能否盈利」**。

**这是 §二.21 里我的判据错误。** 那一轮我说「TS 择时优势复现了」，并把它当作
正面结论 —— 那句话**本身没错**（技巧确实复现），但我**用它代替了盈亏判断**。
两件事之间隔着一个成本，而那个成本恰好是全部问题所在。

#### 净收益随成本（Q1–Q4，6 窗合并）

| 成本 | 均值 | 中位 | 正收益比例 | 有效 Sharpe |
|---|---|---|---|---|
| 0.0bp | +0.89% | +0.62% | 54.6% | 0.064 |
| **2.0bp** | +0.39% | +0.13% | 53.3% | 0.028 |
| **4.0bp** | **−0.11%** | **−0.36%** | 51.4% | −0.008 |
| **7.2bp（实测）** | **−0.91%** | **−1.09%** | 49.2% | −0.067 |
| 12.0bp | −2.09% | −2.20% | 45.3% | −0.156 |
| 20.0bp | −4.02% | −4.00% | 40.0% | −0.308 |
| 50.0bp | −10.94% | −10.94% | 17.8% | −0.919 |

**盈亏平衡费率（中位收益过零）：2.5 bp/边。** 均值口径约 4bp。
**而实测成本是 7.2 bp/边**（$1k 规模，含 taker 费，§二.19）。

**所以在真实成本下这条规则不盈利。**

#### 这条研究线的最终答案

把三步拼起来：

| 测量 | 结果 |
|---|---|
| 择时技巧（配对敞口，成本盲） | **真实且稳定**：6/6 窗口高于 25%，合计 46% |
| 技巧的**价值** | **≈2.5 bp/边**（中位收益过零的费率） |
| 执行的**成本** | **7.2 bp/边**（实测，$1k 规模） |
| 技巧所在的**容量** | 每标的 **$9k–27k**（50bp 内深度，§二.23） |

**技巧真实，但比成本小约三倍；而容纳它的书本又只有每标的 $9k–27k。**
这不是「样本不够」或「参数没调好」，而是**这条效应在成本上不成立**。

**注意这同时也解释了为什么它在 §二.15 看起来那么好**：那一版的绝对收益
（+338% 净合计）是**在 3.5bp 的假设成本下**、且**用原始收益**衡量的。两个错误
方向相反且都不小，所以当时没能看出来。

#### 关于「2.5bp」这个数字的限定

它是**中位标的收益过零**的费率。均值口径是 ~4bp，跨窗中位口径更高一些
（有一窗的中位明显偏负，拉低了均值）。所以准确的表述是
**「2.5–4 bp/边」**，而不是一个精确的 2.5。无论取哪个，都远低于 7.2bp ——
**这个结论对口径不敏感**，所以这里不需要在三个数之间挑一个。

#### 落地

无新模块。这一步用的是已有的 `mean_reversion` / `universe.liquidity_quintiles` /
`trend_gate.random_control`。**所有数字可由 `scan_exits.py` 的判据改动复现** ——
步骤写在上面的表里，成本是唯一变化的参数。

---

### 25. 降低执行成本这条路：缺口是 1.3 倍，但那是靠费率不是靠换手

§二.24 的结论是盈亏平衡 2.5–4 bp/边、实测成本 7.2 bp/边。于是问：**把成本压下去
能不能让它成立？** 而 §二.24 结尾我给了一个估算并写进了文档：

> 「要把 7.2bp 压到 3bp 以下，**换手需要降到现在的约 40%**。」

**这个估算是错的**，这一步首先更正它。

#### 更正：换手不是那条路

我把「每边成本」和「总成本」混了。降低换手**不降低每边成本**，只降低总成本 ——
而策略的毛收益也随交易数同比减少，所以**比例不变**。真正能让盈亏平衡费率上升的
是「每笔交易的毛优势相对每笔成本变大」，那要么来自**更大的毛优势**，
要么来自**更低的每边成本**。

实测各出场阈值的跨窗口盈亏平衡费率（同一批 Q1–Q4 标的，6 窗）：

| exit | 交易 | 均持仓 | @0bp | @7.2bp | 斜率 pp/bp | **平衡费率** | @7.2bp>0 |
|---|---|---|---|---|---|---|---|
| 40 | 12715 | 10.6 | +0.91% | −1.26% | 0.3011 | 3.03 bp | 4/6 |
| 45 | 11525 | 15.9 | +1.32% | −0.66% | 0.2742 | 4.80 bp | 4/6 |
| 50（默认） | 10528 | 23.5 | +0.89% | −0.91% | 0.2496 | 3.56 bp | 4/6 |
| **55** | 9514 | 34.1 | +1.26% | −0.38% | 0.2271 | **5.53 bp** | 4/6 |
| 60 | 8347 | 50.8 | +0.54% | −0.89% | 0.1988 | 2.73 bp | 3/6 |
| 70 | 5434 | 142.8 | **−2.96%** | −3.88% | 0.1280 | **−23.10 bp** | 3/6 |
| 80 | 2438 | 562.2 | **−6.80%** | −7.21% | 0.0569 | **−119.54 bp** | 3/6 |

**换手确实降低了成本斜率**（0.3011 → 0.1988，从 exit=40 到 60），
**但超过 60 之后毛收益本身变负**（exit=70 的 0bp 收益是 −2.96%）——
因为持仓 143 根、562 根时它已经是事实上的多头，不再是均值回归。

**所以「靠延长持有降低换手」在 exit≈55–60 就到头了**，节省约 20–25% 的成本，
而不是我原先估的 60%。

#### 这条路能不能走通

| | 值 |
|---|---|
| 最好的盈亏平衡费率 | **5.53 bp/边**（exit=55） |
| 实测成本 | **7.2 bp/边** |
| **缺口** | **1.3 倍**（需把有效成本降约 23%） |
| 成本构成（§二.19） | taker 费 3.5bp + 执行 3.67bp |

**结论：没有任何出场配置在实测成本下为正，但缺口只有 1.3 倍。**

而 1.3 倍是**可以触及的**：成本的一半是 taker 费。若用 maker 挂单（或返佣），
费率从 3.5bp 降到 ~2bp 就能省 1.5bp —— 单这一项就覆盖 23% 缺口的一半以上。

#### 但这里有个更硬的问题：1.3 倍量级和测量误差同阶

上面七个平衡费率的范围是 **−119.5 到 5.53 bp**，其中 exit 40–60 的五个是
**2.73 ~ 5.53 bp**。**它们之间的排序（exit=55 最好）在这个样本量下不可靠** ——
七个值来自同一份数据、同一组 6 窗口，差异在噪声量级内。

所以**不能**据此推荐「改用 exit=55」——那正是 §二.16/20/22 三次失败的模式
（单样本最优）。能站住的只有**一个定性的结论**：

> **盈亏平衡费率低于实测成本，量级为 1.3 倍。所以「降低执行成本」这条路的
> 收益上限是「刚好打平」，不是「变得有吸引力」。**

而这与另一件事叠加之后更明确：**效应只存在于 50bp 深度 $9k–27k 的档位
（§二.23）**。所以即使把成本压到打平，容量仍然是每标的万元量级。

#### 顺带对账：单窗口与跨窗口差了 12pp

这一步开头发现了一个必须解释的矛盾：§二.15/§二.22 的**单窗口**表给出 exit=50 在
7.2bp 下净均值 **+11.23%**，而 §二.24 的**跨窗口**平均是 **−0.91%**。

在同一批标的、同一窗口上复现，单窗口的 +11.23% 是**准确的**：

| 费率 | 均值 | 中位 |
|---|---|---|
| 0.0bp | +16.81% | +15.68% |
| 3.5bp | +14.06% | +12.93% |
| **7.2bp** | **+11.23%** | +10.04% |
| 14.4bp | +5.93% | +4.60% |

**两者都对，只是那 170 天是一个有利的抽样** —— §二.21 的跨窗净收益是
+122% / **−993%** / −273% / +395% / +108% / +147%，换算成每标的均值正是 −0.95%。

**这正是 §二.21 建立的纪律：不要引用单窗口数字。** 而 §二.22 那张出场扫描表里的
净收益列**全部是单窗口的** —— 它们可以在单窗口内部互相比较，但不能与跨窗口的
数字并列。本轮已按跨窗口重做。

---

## 三、方法教训

这一节比上面的结论更可复用 —— 四次结论翻转全部来自这里。

### 1. 随机对照是唯一有效的判据

出问题的每一项都是被**随机基准**推翻的，而不是被理论或直觉。
随机对照剥离了「暴露度」「市场 beta」「离场结构」这些混淆变量。

**横截面版本**：同票池、同调仓、同成本的随机多空组合。30 次随机中位 −6.60%，
而反转规则 +87.20%。

### 2. 有效样本 ≠ 行数

| 信号 | 行数 | 有效独立样本 |
|---|---|---|
| OHLCV 指标 | 12,168 ~ 21,384 | 数万（但符号翻转） |
| 账户多空比 | 21,384 | **15 ~ 24** |

`n_eff = n × (1−ρ₁)/(1+ρ₁)`。自相关 0.88~0.92 的序列，
**看起来九次独立检验其实只有两三次**。

### 3. 口径必须钉住

`--days 170` 锚在运行时刻 ⇒ 两次运行描述不同时段 ⇒ 数字差异无法归因。
用 `--as-of` 钉住后，同一配置的结果才可比。
**「鲸鱼 PF 持平在 1.21」这个旧结论就是窗口漂移造成的。**

### 4. 门禁静默失效出现了三次

| 次数 | 门禁 | 表现形式 |
|---|---|---|
| 1 | 陈旧窗口 | 鲸鱼账本 3/4 时间为空，不报错 |
| 2 | banner 假声明 | 同一次运行既说「未纳入」又说「已纳入」 |
| 3 | `btc_trend_filter` | BTC 数据集缺失时拦截 0 笔，计数里没有该行 |

共同点：**不产生异常，只产生一个「看起来正常」的数字。**

### 5. 反复出现的失误模式

「**对比口径和我想的不一样**」，同一个模式出现了六次：

| 失误 | 表现 |
|---|---|
| `time.mktime`（本地时区）vs 实现的 UTC | 测试期望值错 |
| `epoch_ms`（裸 TIMESTAMP）vs `TIMESTAMPTZ` | 隐式转换偏移 8 小时，得到真实但错误时段的 bar |
| `Trade.size`（恒正）当作方向 | 「随机基准 100% 做多」的假警报 |
| 单市场 `datasets` 调用 | 静默关掉宏观门禁，交易数对不上才发现 |
| `run()` 返回顺序与解包不一致 | 把换手当成毛收益 |
| 退市分支永不触发 | 「最坏」与「保守」给出完全相同的数字 |
| 出场用 `opens[i]` 而票数用 `closes[i]` | 用还没发生的收盘价出场，9/9 满分（§二.9） |
| `max` 写成该用 `min` 的地方 | 「首个可评估 bar」取成了最后一个，全部运行被拒 |
| 变异脚本改了文件但模块已缓存 | 6 种变异全部「捕获 0」，看起来像测试没用 |
| 变异脚本 `reload` 了子模块但父包仍持旧引用 | 同上，第二次 |
| 断言测自洽性而非关系 | 成交与成交 bar 一起移动，前视偏差照过（§二.9） |
| 测试序列从不走被测分支 | 单调序列只有收尾平仓，出场路径的变异无处可抓 |

最后一条最隐蔽：把 `end` 钳制到最后可得 bar，取价永远成功，
`elif` 分支永不进入。**如果没盯着两列数字相同就会得出错误结论。**

**教训：每个对比都要验证它真的在比我想比的东西。**

---

## 四、实现状态

横截面组合层已落地为 `backtest/cross_section.py`，**与 `engine.Backtester` 相互独立**：
后者是单标的单持仓，前者是多空一篮子，共用一套仓位模型会把「一次一仓」
「单币种冷却」这类假设偷偷带进来。

模块分层：纯计算（`Panel` / 信号 / `CrossSectionBacktester` / 成本）与数据加载
（`load_archive_panel`，走 duckdb）分开，所以测试用合成面板离线跑。

**关键约束：模块必须复现研究数字。** `tests/test_cross_section.py` 里有一条
`TestCrossSectionParity`，用归档重跑非流动子集并断言净收益等于研究报告里的
`+87.20% / +7.37% / +78.05% / +86.32%`（容差 0.5）。研究数字出自一次性脚本，
一次静默改了数字的重写会让基于它的结论全部失效。

模块验证时发现的额外结果：流动子集上同规则的净收益是 **−50.94% / −66.26%**，
落在随机的第 3 / 第 0 百分位 —— 比之前 IC 口径下 t≈−1 的「测不出」更强，
说明在效应不存在的地方这条规则是**主动亏钱**的。

**选池层已落地为 `backtest/universe.py`（2026-09-19）**：标的间相关性、
两种有效样本修正（§二.7）、传递闭包聚类、按流动性一簇取一个，外加现货对过滤。
纯标准库（项目无 numpy，不为一个功能引入），44 条离线测试。

**趋势门槛已落地为 `backtest/trend_gate.py`（2026-09-19）**：四个条件的
布尔值（`vote_detail`，逐个可断言）、单一阈值仿真、配对敞口的随机对照。
同样不接入 `engine.Backtester` —— 那条路径不支持「够票才做多」的门槛语义，
硬塞进去会把「规则能否独立成立」变成「规则能否和一堆门禁一起成立」。
37 条离线测试，其中 `TestNoLookAhead` 是变异验证过的（把成交挪前一根，1 个测试失败）。

`simulate_flags` 是这套东西里真正承重的部分：成交时点、双腿手续费、逐 bar
资金费、以及「信号 bar 与成交 bar 分开记录」。**新规则必须复用它，不要写第二套
模拟器** —— 那个循环错一次就把亏损规则变成了 9/9 满分。

**回调入场已落地为 `backtest/pullback_entry.py`（2026-09-19）**：复用
`simulate_flags`，只新增条件的布尔计算与一份配置（`PullbackConfig`）。
54 条离线测试。三处规格留白（哪个 EMA、穿越还是状态、3% 有没有下界）
各自可配，并在结果里一并报出，不静默取值。

**均值回归已落地为 `backtest/mean_reversion.py`（2026-09-21）**：RSI 与 Keltner
两套规则、Keltner 通道、可配 ADX 趋势过滤，复用 `simulate_flags`。
25 条离线测试（6/7 变异捕获）。**这是目前唯一毛收益为正的模块**（§二.15）。

一条贯穿的约定：**成交时点只有一份实现**（`trend_gate.simulate_flags`）。
四个信号模块都复用它，因为那个循环错一次就把亏损规则变成了「9/9 满分」
（§二.9）。任何新规则都应当接入它，而不是另写一套模拟器。

**组合分析已落地为 `backtest/combine.py`（2026-09-22）**：`correlation` / `sharpe` /
`risk_parity_weights` / `blend` / `weight_sweep` / `halves` / **`stability`**。
22 条测试，8/8 变异捕获。

其中 `stability()` 的设计意图值得单独说：它把**全样本与前/后半放在同一次返回里**，
所以调用者不可能只拿到全样本数字就写进结论。**本仓库两次失败（§二.16 止损、
§二.20 叠加）都发生在这个位置，而两次被引用的都是全样本。** 把这条纪律做成 API
的形状，比写成一句警告有效。

**微观结构诊断已落地为 `backtest/microstructure.py`（2026-09-22）**：自相关 /
自协方差、`roll_spread`（有效价差的上界）、`variance_ratio`、`reversal_profile`、
`bounce_share`。纯标准库，24 条测试（7/7 变异捕获），**精确复现手算的归档数字**。

它回答的是「这个效应会不会只是价差在动」（§二.18），**不回答「它是否可交易」**
—— 后者只能把费率设成价差重跑。模块文档明确写了这条边界，因为把两者混为一谈
正是 §二.18 差点得出的错误结论。

它**不接入 `engine.Backtester`**，理由与横截面层相同：那是单标的单持仓引擎，
选池层产出的是一个票池，两者的接口是「配置里的 symbols 列表」，不需要改仓位模型。
所以这一层可以立刻用于实验 —— 它改变的是喂给引擎什么，不是引擎怎么跑。

**信号的数据源不变量已修（2026-09-22）**：在测试网上走管道时发现
`technical`(35%) 与 `market`(25%) 读的是**执行客户端**，于是 `testnet: true` 时
60% 的因子权重来自测试网数据 —— 而 README 一直写着「`testnet` 只控制订单发往哪里」，
那条规则此前只在 `smart_money` 上实现过。

实测（同一时刻、同一标的、同一配置）：BTC 资金费 **0.30 → 8.12 bp/h**
（26.2% → 711.3% APR），未平仓 **$3.95B → $4.74M**，`market` 因子 **51.9 → 37.0**，
合成信号 confidence **85% → 58%**。资金费是打分项（crowding 权重最高 0.65），
代码注释只豁免了未平仓、没有豁免它。

**这条与本研究线的关系**：它不影响上面任何一条回测结论（`backtest/` 的市场对象
`testnet = False`，主网下 `mainnet_data_market` 返回同一对象、行为逐位不变）。
它影响的是「用测试网排练主网行为」这件事本身 —— 而那正是**下一步唯一可做的前向
验证**（§六第 2 条的第三个选项）。所以它必须在跑测试网之前修掉，而不是之后。

规则抽成 `market_data.mainnet_data_market()`（此前在三处各写一遍），三个因子、门禁、
以及止损距离用的 ATR 走它；订单路由 / 账户权益 / 标记价 / 对账仍走执行客户端。
三个因子统一记录 `data_network`，并修掉一处同类问题：`technical` 在「K 线不足」时
返回的 `FactorScore` **没有任何 `details`** —— 最需要说明数据来源的情况恰好是唯一
不说的。865 测试通过（新增 12 条），11 项变异全部捕获。

## 五、尚未测量

| 项 | 为什么没测 |
|---|---|
| 归档自身完整性 | 需与 S3 源比对；退市标的的数据是否整体丢弃无法从本地判断 |
| ~~横截面效应的样本外~~ | **不是没测，是拿不到数据** —— 见下方「数据已被取尽」 |
| 组合层实现 | 现策略单标的单持仓，横截面需要多空一篮子 —— 是重写不是调参 |
| `fills` 的更多用途 | 只有三列（address, timestamp, realized_pnl），无方向/数量/币种 |
| funding 归档 | 归档不含资金费历史，长窗口仍走 API |
| ~~分散票池上的方向性回测~~ | **已测（§二.8）**：有效观测 64 → 177，PF 1.22 → 1.10，11/31 超 p75 不显著 |
| **相关性随时间的稳定性** | 只测了一个窗口的静态相关矩阵。相关在压力期会一起上升，那正是过滤最该起作用的时候 —— 需要滚动窗口才知道阈值该设多少 |
| 现货对是否只影响选池 | 已确认它们不该进票池，但**未确认**它们是否也污染了 `cross_section` 的历史结论（那批研究用的是归档全量） |

### 数据已经被取尽（2026-09-19 确认）

这一条比上面几项都重要，因为它决定了**哪些问题还能被测**：

```
python sync_reservoir.py --datasets candles --dry-run --profile s3reader

  dataset    to fetch   have     source
  candles           1    414     0.04 GB
```

**源上只有 415 天，本地已有 414 天 —— 归档已经取完。** 归档同时包含
**6749 个标的**（单日 882 个），其中 **3808 个中途停止交易**、6278 个较晚上市，
所以退市标的确实在内，这一点不是问题。

结论：**「横截面效应的样本外检验」在这个数据源上无法完成。** 它不是"还没做"，
是"做不了"。任何基于这 414 天的进一步信号搜索，都落在 §六 警告的那种多重比较里。

唯一能替代的，是在这 414 天内做**更多不重叠分段**（§2.6 已做 6 段，3/6 过 p75）。
但分段越多，每段越短、期数越少，「显著」就越容易是抽样 —— 这不解决问题，只是
把同一个问题分成更小份。

#### 顺带修正一处口径错误（我自己犯的）

初次检验退市敏感性时，我用「覆盖 ≥350 天」筛选标的，得到 −4.27%，据此怀疑
研究有幸存者偏差。**这个结论是错的**：`present(60)` 才是研究的口径（刻意保留
退市标的），而「覆盖 ≥350 天」**恰好把退市标的筛掉了** —— 我用一个幸存者票池
去指控别人幸存者偏差。

在研究自己的口径上复现（`present(60)`，85 个非流动标的，14 个中途退市）：

| 配置 | 研究报的 | 复现 | 退市归零 | 按最后价 |
|---|---|---|---|---|
| hold=42, lag=42 | +87.20% | **+87.20% ✓**（p97） | +87.20% | +107.51%（p100） |
| hold=42, lag=6 | +7.37% | **+7.37% ✓**（p73） | +7.37% | +22.62%（p90） |

所以退市假设只移动 15–20pp，**不是决定性的**。

**但浮现了两个更该注意的问题：**

1. **同一持有期只换信号滞后（42 → 6），结果从 +87.20% 掉到 +7.37%（p73，没过线）。**
   两条都在等价性测试的表里，也就是说这个结果的参数敏感性是已知的。
2. **策略的「典型一期」比随机更差**：`(42,42)` 策略中位/期 **0.965%**，随机中位/期
   **1.42%**。+87.20% 来自复利尾部，不是来自更高的典型期收益。这一条此前没有被指出。

---

## 六、下一步

唯一能提升统计功效的方向是**扩大横截面**（已验证有效），不是延长时间。

在组合层就位之前，**不建议再对现有数据做新的信号搜索** ——
每次搜索都在有限的独立观测上做多重比较，找到「显著」几乎是必然的。

**2026-09-19 补充：数据已取尽，这一节的性质变了。**

上面第 1 到 5 条仍然成立，但它们都假设「还可以再做新的测量」。**现在不能了**：
源上只有 415 天，本地已有 414 天（§五「数据已经被取尽」）。

所以这一节现在的建议是：

**1. 停止在现有数据上搜索新信号。** 这不是「最好别」，是「做了也不产生信息」——
414 天是一个样本，分段只会让每段更短。§二.7、§二.8 已经把这条路走完了。

**2. 要判断现有唯一那个候选效应，需要换数据源，不是换参数。**
横截面反转的样本外检验在这个源上做不到。可选方向只有：更长的历史（另一个
数据源）、另一个市场、或前向（等实盘/纸面运行累积新数据）。

**3. 在拿到新数据之前，不要再动策略参数。**
§二.8 已经证明「扩大样本」不会让效应出现，反而让它更小（PF 1.22 → 1.10）。
方向性路径上剩下的唯一动作是**组合层重写**（多空一篮子），而它的唯一依据
（§二.6）是 6 段里 3 段过 p75、`P(X≥3) ≈ 17%` —— **不显著**。
**为这个依据做重写，风险高于收益。**

**4. 三个当时没查、现在值得低成本补齐的：**

| 项 | 为什么值得 |
|---|---|
| ~~现货对是否污染了 `cross_section` 的历史结论~~ | **已复现并固化** —— 见上，排除现货对不改变 3/6 |
| `(42,42)` 与 `(42,6)` 的 12 倍差异 | 两条都在等价性测试表里，说明参数敏感性已知但未被当作风险记录 |
| 策略中位/期低于随机中位/期 | `(42,42)`：策略 0.965% vs 随机 1.42%。+87.20% 来自尾部复利而非典型优势 |

#### §2.6 的分段研究已复现并固化（2026-09-19）

查遍仓库时发现：`coin NOT LIKE '@%'` 这个排除**只出现在 `tests/test_cross_section.py`**
的等价性测试里，而 **§2.6 分段研究的代码不在仓库** —— 它是「一次性脚本」。
这正是 §四 那条「模块必须复现研究数字」要防的事，只是防住了主结论、
没防住分段那一层。**「6 段里 3 段过 p75」的输入票池当时无法重建**，
而它正是 §六 第 3 条「要不要做组合层重写」的唯一依据。

已用 `slice_rows` + `NOT LIKE '@%'` 重跑并**完全复现**：

| 段 | 标的 | 期数 | 净收益 | 中位/期 | 随机中位 | 百分位 |
|---|---|---|---|---|---|---|
| 1 | 85 | 8 | −1.00% | −0.320% | +4.10% | 37 |
| 2 | 84 | 8 | −10.31% | −1.288% | +3.36% | 23 |
| 3 | 83 | 8 | +9.34% | +0.427% | −2.60% | **77** |
| 4 | 82 | 8 | +10.06% | +0.500% | −1.86% | **87** |
| 5 | 78 | 8 | +34.32% | +4.014% | −1.67% | **97** |
| 6 | 74 | 8 | +2.91% | +0.517% | −1.77% | 70 |

**3/6 过 p75，`P(X ≥ 3) = 0.169`** —— 与研究报的 3/6 / ≈0.17 一致。
排除现货对不改变这个结论。

已固化为 `tests/test_cross_section.py::TestSegmentStabilityIsReproducible`：
排除写进 SQL，**每段收益逐条断言**（不只断言 3/6 那个计数 —— 若某个改动让
两段数字互换而计数不变，只查计数会漏掉，这正是 §三.5 的模式）。
运行约 35 秒。

**顺带确认**：`load_archive_panel` / `build_panel` 本身**不**过滤现货对
（它们只处理传入的币种）。调用方必须显式过滤，否则 `2e-07` 价格上的收益率
会进入面板。

**5. 如果目标是「让这个项目产生价值」而不是「找到 alpha」**，那么当前状态
（一个完整的、可复现的、自带随机对照与失效检测的研究框架，外加一个 dry-run
的 agent）本身已经是产出。继续调参不会增加它。

**6. 「换一套指标重来」这条路已经走过（§二.9）。** 四项纯趋势硬门槛是
**独立于现有信号**的新规则，假设空间小（4 个条件 + 1 个阈值），结果
**9 格中 0 格跑赢买入持有**。所以「现有指标不行，那就换指标」这个方向
有了一次直接检验，答案是同样不行。

值得注意的是失败模式变了：老策略的问题是「优势测不出」，而新规则是
**主动亏钱**（7/9 格为负）—— 因为 36–60% 的敞口让掉了趋势涨幅却照付手续费。
趋势跟随在一个**已经上涨 15–21%** 的窗口里输给买入持有，这本身是正常的，
但输 10–18 个百分点是成本结构造成的，不是运气。

**7. 优先级被 §二.10 改变了：成本先于信号。** 主策略毛 PF 1.29 / 净 1.22，
趋势门槛 14–16pp 的拖累 —— **每一条既有结论都是在扣掉 12–16pp 的成本之后
得出的**，而这个成本此前从未被单独报出。所以顺序应该是：

1. **先问「这个换手率配得上这个毛优势吗」**，再问信号有没有效。
   在趋势门槛上，把换手从 228 降到 49 就直接把净值从 −7% 变成 +16%
   （BTC），而信号一个字没改。
2. **把「毛/净」和「按敞口归一化」写进每一次回测的输出。** 前者区分
   「优势被成本吃掉」与「本来就没优势」，后者区分「63% 敞口赚 15%」与
   「100% 敞口赚 15%」。缺了这两项，任何数字都可以有两种读法。
3. **降低换手不会创造 alpha**，但会让剩下的测量有意义 —— 趋势门槛的对照
   显示，降换手后的收益与「随机入场 + 同样持仓时长」无法区分。所以它是
   **必要条件，不是充分条件**：不解决它，测的是手续费；解决了它，才轮到
   测信号。

**8. 回调入场是唯一值得继续的方向（§二.11），但要按它的弱点来测。**

它是第一个通过换手率检验、且方向与已验证效应一致的规则（7/18 分段超 p75，
好于此前一切）。但它的证据目前是**一个标的（ETH）、一个窗口**，而且
BTC/SOL 的全部收益来自单笔最佳交易。

要让它从「值得继续」变成「成立」，需要的是它现在缺的东西，不是更多参数：

| 缺什么 | 怎么补 |
|---|---|
| **跨标的的普适性** | 在 §二.7 的分散票池（ρ≥0.7，31 个）上跑，而不是三个高相关标的。ETH 单点突出正是「一个标的一次运气」的形态 |
| **单笔依赖** | 已在报「去掉最好一笔/前二笔」；应作为常规输出，而不是这次的特例 |
| **出场是留白** | 目前用「跌破中线即离场」。§二.10 已证明出场决定成本结构 —— 应把出场当一等参数扫，而不是沿用入场规则的镜像 |
| **3% 上限几乎无效** | 实测只筛掉 6.6%（93.4% 的 bar 本就满足）。要么去掉它（规则更简单），要么说明它为什么仍要保留 |

**10. 均值回归是当前唯一值得继续的方向，但要按它的弱点测（§二.15）。**

RSI/Keltner 是第一个毛收益为正、分段显著的规则（毛 +446%、112/216 超 p75、
修正 P=0.009）。但它有三处已知弱点，每一项都有明确的测法：

| 弱项 | 说明 | 怎么测 |
|---|---|---|
| **绝对收益低于买入持有** | 中位 +4.4% vs +12.4% | 它赢在风险调整后。要判断这是否够，需要一个明确的**目标函数**（夏普？回撤？），而不是只看收益 |
| ~~尾部依赖重~~ | ~~去最好一笔后 +338% → +62%~~ | **已测（§二.16）**：止损截断左尾但效应测不出，不采纳 |
| **下跌窗口修正后不显著** | P≈0.26 | 含止损的版本也没解决（§二.16）—— 止损同样测不出 |

另外两件尚未做、且成本不高的事：

- ~~**横截面版本**（买 RSI 最低的一组、空最高的一组）~~ **已测（§二.17）**：
  它是既有反转效应的较弱形式，**不替代基线**；而时序版经 2×2 证实是**独立效应**，
  不是它的劣化近似。
- **出场扫描**。当前出场是「RSI 回到 50」或「价格回到 EMA20」，两者都是
  入场规则的镜像。§二.10 已证明出场决定成本结构，值得当一等参数扫。

**注意：绝对收益这一项现在成了唯一未被处理的主要弱项。** 在解决它之前，
「均值回归有效」这句话的准确形式是：**它在风险调整后优于同等敞口的随机择时，
但绝对收益不如买入持有。** 而这两者哪个重要，取决于目标函数 —— 那是一个需要
先回答的问题，不是能靠继续跑回测绕开的。

**§二.17 之后新增并已排在最前面的两项：**

1. **时序择时 + 横截面选股能否叠加**（§二.17）。两者已被证明是不同效应，
   所以叠加是**有理由的假设**而非又一次盲试。注意这不是把两个回测相加，
   需要一个新的组合层。
2. ~~**非流动一侧是否只是微观结构假象**~~ **已测（§二.18）：不是假象。**
   但发现了一个**更具体的问题**：效应在 hold=1 为零、在长持有期才为正，
   而长持有期版本按悲观价差假设显著性只剩 t≈0.7–1.5。**§二.17 的正收益是
   成本乐观口径**，这一点需要回填到 §二.15 和 §二.17 的结论里。

**§二.21 之后：只剩下一个方向，而且判据已经换对了。**

跨窗口重复给出了本仓库第一个复现的正面结果：**TS 的超 p75 在 6/6 个独立窗口高于
25%（符号检验 P=0.031）**。同时它否掉了两样东西：§二.17 的横截面（t>2 仅 1/6）
与 §二.20 的叠加（组合 Sharpe>1 仅 3/6）。而 §二.20「TS 无效」的判断是**换了尺子**
造成的 —— 用含市场 beta 的原始收益去否定一个配对敞口的择时优势。

所以：

1. **判据统一为「超 p75」**（配对敞口的随机对照）。原始收益不稳定是**结构性的**
   （它含 beta），不是效应的性质。任何用原始收益 Sharpe 否定 TS 的结论都要重读。
2. **不要再碰横截面组合。** 它没通过跨窗口，而组合建立在它之上。
3. **把 TS 规则本身做扎实**，按性价比：
   - ~~**出场扫描**~~ **已做（§二.22）：没找到改进，默认 exit=50 是最稳的。**
   - ~~**跨标的验证**~~ **已做（§二.23）：效应有流动性上限，止于最流动的一档。
     而效应所在的四档，50bp 深度只有 $9k–27k。**
   - **「超 p75」对成本的敏感性表**。§二.19 只给了 $1k/$10k 两个点，
     §二.23 给了分档的容量，但**没有把「超 p75 的比例」画成成本的函数** ——
     那才是「这个择时优势能承受多贵」的完整答案。

**§二.23 之后，参数空间和标的空间都探完了**：入场（§二.15）、出场（§二.22）、
过滤（ADX）、成本（§二.19）、组合（§二.20/21）、标的池（§二.23）都测过，
**只有默认值在跨窗口上成立**，而效应**只存在于低容量档位**。

所以下一步不该是继续在这份归档上做实验。两个方向，都是「换问题」而不是「调参数」：

1. ~~**把「超 p75」画成成本的函数**~~ **已做（§二.24）**：那个指标是成本盲的，
   真正回答成本问题的是净收益 —— **盈亏平衡 2.5–4 bp/边，实测成本 7.2 bp/边。**
2. **接受结论并停下来。** 本仓库现在有一份完整的答案：

> **一个在 6/6 窗口复现的择时技巧（46% 超 p75，成本盲），它的价值是
> 2.5–4 bp/边，而执行成本是 7.2 bp/边；它只存在于 50bp 深度 $9k–27k 的档位；
> 并且在 §二.22 的出场扫描里，任何参数改动都不比默认更稳。**
>
> **所以这条效应在成本上不成立。** 不是样本不够，不是参数没调好，不是
> 容量之外还有空间 —— 三件事都测过了，方向一致。

**继续调参只会找到第四个不跨窗口的最优**（§二.16/20/22 已经三次）。

**§二.25 之后，「降低执行成本」这条路也已经量完了**：缺口 1.3 倍，且能动的
只有费率那一半；跑通了也只是打平。所以本方向**已经封闭**：入场、出场、过滤、
成本、组合、标的池、执行成本，七条都测过，方向一致。

**下一步（若还要走）必须是换数据，不是换参数或成本假设**，且要满足三个条件：
更大容量 **且** 更低成本 **且** 效应时长跨窗口 —— 本归档里同时满足的标的不存在
（§二.23 的表里没有一行）。所以诚实的建议是**停在这里**，除非拿到新数据
（更高频的 bar、盘口历史、或另一段市场）。

**若要继续这条研究线，需要的是换输入，不是换参数**：更高的频率（更多不相关的
机会）、能同时提供容量与低成本的标的（本归档里不存在）、或者一个**成本更低**
的执行方式。

> **⚠ 2026-09-22 更正（§二.25）：这里原先写的「把 7.2bp 压到 3bp 以下，换手需要
> 降到现在的约 40%」是错的** —— 我把「每边成本」和「总成本」混了。降低换手
> 不降低每边成本，而毛收益也随交易数同比减少，**比例不变**。
>
> 实测（§二.25）：靠延长持有能把成本斜率降 20–25% 就**到头了**（exit>60 之后
> 毛收益本身变负，因为它变成了事实上的多头）。最好的盈亏平衡费率是 **5.53 bp/边**，
> 而实测成本 **7.2 bp/边** —— **缺口 1.3 倍**。
>
> 所以真正能动的是**费率那一半**（taker 3.5bp），不是换手。跑通了也只是
> **刚好打平**，不是变得有吸引力。

**仍未被解决的**仍是 §二.15 那条：**绝对收益低于买入持有**。但现在它的形状完全清楚了
——**TS 是一个低敞口的择时优势（配对敞口口径稳定），其绝对收益必然低于满仓持有**
（敞口只有约 16%）。这不是缺陷，是它的定义。所以唯一要回答的问题是：
**这个择时优势值不值它的复杂度**，而那取决于目标函数，不是回测。

**11. 不要退回趋势跟随。** §二.9 的四票门槛与 §二.12 的 EMA9/26 金叉都是
**延续**规则，前者毛收益 ≈ 0、后者毛收益为负，而 §二.15 的**反转**规则毛收益为正。
这个对比是目前手上最干净的方向性证据。

**9. 跨标的验证现在是拦路的问题，不是可选项（§二.12 之后）。**

ETH 在**每一版**规则里都是唯一亮点（+29.56% → +35.90%），而 BTC 和 SOL 在任
何一版里都不成立。三个标的相关 0.86，所以这不是「ETH 更适合这个策略」，
更可能是**这个标的在这个窗口**的属性。

判别方法只有一个：把 §二.11 的规则（不是 EMA9/26 那版 —— 分段证据已经否掉它）
放到 31 个分散标的上跑，看 ETH 的表现是分布中的一个点还是分布外的一个点。

**在此之前不要再调整 BTC/ETH/SOL 上的参数** —— 每调一次都是在同一个标的上
做一次多重比较，而 §二.12 已经演示了这种调整会让头部数字变好、证据变差。

**不要做的**：继续加条件。§六 开头那条「不建议再对现有数据做新的信号搜索」
仍然成立 —— 上面四项里有三项是**用已有规则在更多标的上验证**，只有出场扫描
算新搜索，而且它是被 §二.10 的证据要求的（换手率决定成本）。
